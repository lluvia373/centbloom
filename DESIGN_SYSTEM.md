# 화면 디자인 기준

최종 수정: 2026-09-24 · 새 화면과 수정하는 화면 문구·스타일의 기준. 기존 화면 전체의 준수 완료를 뜻하지 않는다. 실제 결과는 [PROJECT_STATUS](./PROJECT_STATUS.md#검증-기록).

역할: 현재 화면 문구·글꼴·크기·색상·간격·이미지 사용과 검증 기준. 기준이 바뀌면 해당 항목을 교체하며 시안·수정 경위를 누적하지 않는다. 승인 로고 자산은 [BRAND](./BRAND.md), 코드 소유 위치는 [DEVELOPMENT](./DEVELOPMENT.md#스타일자산도구), 관리 규칙은 [AGENTS](./AGENTS.md#문서-관리-기준)를 따른다.

## 기본 방향

**서비스 핵심(D015):** 자연스러운 흐름과 좋은 사용감을 최우선 기준으로 삼는다. 같은 역할은 같은 동작을 제공하고, 클릭 후 결과와 돌아오는 경로를 예측할 수 있게 한다. 개별 요청을 적용할 때 주변의 같은 요소와 모바일 흐름까지 검토한다. 사용감을 해칠 수 있는 요청에는 이유와 대안을 제안하며 최종 선택은 사용자가 한다.

**완료 기준:** 정확성·접근성을 유지하는 범위에서 사용자 경험은 구현 편의와 타협하지 않는다. 변경 영역의 실제 PC·390px 화면에서 핵심 정보가 먼저 읽히는지, 긴 이름 때문에 불필요하게 행이 늘어나는지, 보조 그래프가 표의 폭을 빼앗거나 긴 빈 열을 만드는지, 검색·초점 전환으로 조작부가 밀리는지 확인한다. 같은 역할의 글꼴·크기·간격과 터치 영역도 함께 확인한다. 코드 검사만 통과하거나 부품 각각이 예쁜 것은 완료가 아니다. 실제 확인 범위와 남은 한계를 진행판에 남기며 ‘최적’이라는 사용성 검증 결과로 과장하지 않는다.

- 사용자가 해당 요소에 명시적으로 요청하지 않은 펼침·접기는 추가하거나 재도입하지 않는다. 설명이 길다는 이유로 details/summary·아코디언·설명 열기 버튼을 만들지 않는다. 필요한 기준·오류만 짧게 바로 표시하고 불필요한 설명은 삭제한다. 긴 목록은 이동·분할을 우선 검토하며 사용자가 요청한 보유종목의 ‘더 보기’는 항목 추가 표시로 구분한다. 기존 주요뉴스·시간표·도움말 등의 접힘은 현재 구현이지 다른 화면에 허용된 예외가 아니며 새 화면의 기본 패턴으로 복제하지 않는다.
- **상태가 바뀌어도 공통 조작부의 자리와 크기는 유지한다.** 모드 전환·검색 결과·항목 추가/제거가 눌렀던 버튼, 공통 필터, 차트 시작선을 밀지 않게 한다. 조건부 검색/목록은 독립 영역에 배치하며 안 보이는 큰 빈 공간을 예약해 해결하지 않는다. 좁은 화면의 재배치는 화면 폭을 기준으로 일관되게 적용한다. 바뀐 대표 상태 전후를 비교해 확인하고 정지 화면 하나만 보고 완료하지 않는다.
- 밝은 회색 배경, 흰 표면, 진한 글자. 한 화면의 같은 역할은 같은 크기·색·여백을 쓴다.
- **최소한의 화면에 필요한 정보는 빠짐없이 둔다.** 사용자의 행동·판단에 필요 없는 범위 부연·미지원 항목·세부 계산 설명은 생략하고 도움말·툴팁으로도 옮기지 않는다. 보유자산 요약에는 ‘주식·ETF’, ‘현금 제외’, ‘환율 포함’, 일반적인 조회 과정은 상시 덧붙이지 않는다. 오늘 손익에는 한국시간·KST·자정 설명을 상시 표시하지 않으며 통화별 자정/현재 시각·출처 진단 목록은 제품 화면에 넣지 않는다. 핵심 수치·필요한 행동·실제 누락/오류는 유지한다. 종목 수는 목록 분할 등 필요한 곳에만 표시하고 제목·탭에는 덧붙이지 않는다.
- **평가액 전용 지연 환율 예외:** 승인된 마지막 정상 환율로 평가액을 표시할 때만, 실제 사용한 지연 통화의 적용 날짜·시각(KST)을 기존 caption·muted 스타일의 짧은 안내 한 줄로 둔다. 정상 환율·미사용 통화에는 추가 안내를 표시하지 않는다. 별도 배지·진단 목록·툴팁·펼침을 만들지 않으며 오늘 손익의 기준시각 상시 표시는 추가하지 않는다. 적용 범위·CSV·엄격한 손익 계산 기준은 [환율 명세](./PRODUCT_SPEC.md#보유자산-개선-검토)를 따른다.
- 강조는 제목·주요 행동·상태에만 사용한다. 임의의 보라색 그라데이션, 발광, 장식 그림자, 이모지 아이콘을 덧붙이지 않는다.
- 금색 장미는 브랜드 식별용이다. 장미의 금색을 모든 버튼·차트에 반복하지 않는다.
- 로컬 개발 서버의 브라우저 탭 제목은 페이지 이동 후에도 `localhost:3000`으로 통일한다. 배포 환경의 서비스명·페이지별 제목과 화면 안의 브랜드 표시는 유지한다.
- 사용자 게시 이미지·기업 로고의 고유색은 보존한다. 주변 배치·비율·여백으로 통일한다.

## 기능 유형별 화면 기준

새 기능은 아래 가장 가까운 유형과 기존 공통 요소를 먼저 사용한다. Wanted Sans·밝은 중립색·절제된 강조·간결한 문구를 공통으로 유지하며, 별도 분위기나 색상 체계를 기능마다 만들지 않는다. 기능별 값·계산·동작은 PRODUCT_SPEC에, 이 유형과 명세의 연결은 [작업별 시작점](./DEVELOPMENT.md#작업별-시작점)에 한 번만 둔다. 아래는 새 작업의 기준이며 기존 화면 전체 준수 확인은 아니다.

| 화면 유형 | 배치·동작 기준 | 기존 참고 요소 |
| --- | --- | --- |
| 목록·순위·뉴스 | 카드 밖 제목, 카드 안 목록, 동일 역할의 제목 조작 통일. 핵심 정보는 바로 노출하고 긴 목록의 이동·분할을 우선 검토. 기존 뉴스 펼침은 현재 구현이며 기본 권장 패턴은 아님 | HomeSection·MoverTable·MarketNews |
| 보유자산 요약 | 현재 요약과 바로 아래 기간 선택·그래프를 하나의 `portfolio-overview` 흰 카드로 연결한다. 넓은 화면은 카드의 가용 폭을 같은 너비의 두 영역으로 나눈다. 왼쪽에는 총 보유자산·평가손익·매입원가, 오른쪽에는 오늘 손익·종목 손익·환율 손익을 둔다. 두 영역 사이는 32px이며 각 묶음 내부는 왼쪽 정렬과 가까운 간격을 유지한다. 오늘 손익 묶음은 왼쪽 묶음의 높이 중앙에 맞춘다. 카드 좌우 여백은 PC 24px·모바일 16px로 유지한다. 세로 구분선·동일 폭 3열·내부 개별 카드는 두지 않는다. 총액은 metric 32px로 가장 크게, 오늘 손익 합계는 section 18px, 평가손익은 body 15px로 구분한다. 수익률은 평가손익 금액 옆에 caption으로 묶고, 매입원가는 PC·모바일 모두 평가손익 바로 아래에 caption 12px·회색 보조 글씨로 둔다. 큰 보유자산 금액 옆에는 원가를 붙이지 않는다. 오늘 손익은 제목과 합계를 한 줄로 붙이고, 그 아래 종목 손익·환율 손익을 caption 12px의 가까운 보조 줄로 묶는다. 독립 연산자(+·=), 계산서형 합계선, 요인별 카드·양끝 정렬은 쓰지 않는다. 금액의 실제 양수·음수 부호는 유지한다. 600px 이하에서는 총액 → 평가손익 → 오늘 손익 순으로 쌓고 오늘 손익 이름 옆에 section 18px 합계를 둔다. 구성은 바로 아래 같은 왼쪽 시작선에 배치하고 요인 단위로 줄바꿈한다. 위아래 여백은 16px, 모바일 오늘 손익 묶음 앞은 12px, 합계와 구성 사이는 8px다. 긴 금액 글자를 일괄 축소하지 않는다. 종목 수는 요약·내용 제목·상세 탭에 표시하지 않는다. 전량 매도 기여와 실제 누락/오류 상태는 유지한다. 요약 아래 공통 설명 줄·별도 기간 성과 제목·성과 이동 버튼은 두지 않으며 선택 기간을 바꿔도 현재 수치는 바뀌지 않는다. 한국시간·KST·자정 설명, 추정 라벨, 환율 적용 날짜·시각·출처 목록은 요약에 반복하지 않고 계산 기준은 명세에 유지한다. 단, 평가액 전용 지연 환율을 실제 사용한 통화는 위의 승인된 한 줄 시각 안내를 적용한다. 해당 종목의 오늘 손익 아래 필요한 일별/최종 수신 적용 날짜는 유지하되 정상 휴장 시세에는 대체 안내를 붙이지 않는다. 자료가 없으면 통화와 실패 시점을 짧게 표시한다. 새 배지·장식 아이콘·툴팁·펼침·접기는 추가하지 않는다. 전체 구성 도넛·보유목록·기간 성과는 아래 배치 기준을 따른다. 모바일에서도 비중/원가를 유지하고 목표 비중 비교·반복 요약은 두지 않음 | PortfolioMetrics·HoldingsComposition·portfolio.css·PortfolioHoldings.module.css |
| 상세·분석 | 제목/복귀 → 핵심 수치 → 차트/세부 자료. 해석에 필요한 단위·기준·누락만 수치 곁에 짧게 표시하고 불필요한 계산 설명·펼침·접기는 추가하지 않음 | StockDetail·PerformanceAnalytics |
| 검색·관심목록 | 공통 검색은 종목 정보 조회, 관심종목 검색은 목록 추가임을 라벨과 입력 안내로 구분한다. 시장 필터 위에 시장 이름을 다시 나열하지 않는다. 빈 목록은 추가 행동·검색과 빈 상태 한 번만 남기고 0 요약·목록 제목 숫자·시세 도구를 숨긴다. 종목이 있으면 목표가·시세 실패/재시도를 유지하고, 시세 실패를 목표가 도달로 표시하지 않는다. 추가 뒤 검색 초점을 유지함 | StockDiscovery·WatchlistSearch·WatchStockButton |
| 입력·편집·노트 | 실제 label·명확한 저장/취소·입력 유지·저장 실패 복구. 노트가 없으면 0 지표·목록 도구를 숨기고 작성 행동·브라우저 저장/삭제 주의를 남긴다. 기록이 있을 때만 검색·분류·정렬을 제공하고 기록 없음과 검색 결과 없음을 구분한다. 로딩·저장 실패는 빈 기록으로 표시하지 않는다. 거래 단가·수수료 편집에는 저장된 통화 단위를 함께 표시한다. 대화상자는 초점 유지·Esc·닫은 후 원래 버튼 복귀를 공통 기준으로 삼음 | JournalPage·TransactionForm·TransactionEditor·HoldingManagement |
| 캘린더·시간표 | KST·날짜 선택·필터·목록/상세 연결. 데이터 미연결과 실제 일정 없음 구분. 작은 화면은 정보 우선순위를 유지하며 재배치 | UpcomingCalendar·CalendarBrowser·MarketSessions |
| 로그인·설정 | 필요한 순간 로그인, 실제 계정 정보·작은 통화 선택·접힌 데이터 관리. 기능 나열·개발 설명은 넣지 않음 | AuthGate·AccountSettings |
| 읽을거리 | 읽기 쉬운 내부 폭과 본문 줄 높이, 제목·출처/작성 맥락·관련 행동. 목록 페이지는 페이지 제목 한 번과 글 제목으로 시작하며 같은 리서치 가이드 제목·현재 페이지로 돌아오는 가이드 전체 링크를 반복하지 않는다. 다른 화면에 넣는 섹션형 ReadingShelf의 제목·전체 링크는 유지한다. 홈처럼 카드나 지표를 억지로 채우지 않음 | ReadingShelf·/read 본문 |
| 광고 | 콘텐츠와 구분된 광고 표기, 본문/조작을 가리지 않음. 오른쪽 사용은 확정, 추가 위치·모바일 형식은 미정 | [광고 명세](./PRODUCT_SPEC.md#광고-배치) |

빈 상태·로딩·부분 실패·재시도는 같은 위치에서 구분한다. PC·모바일·키보드에서 시작 → 완료 → 복귀를 확인한다. 참고 요소에 남은 기존 위반을 새 기능에 복제하지 않는다.

### 종목 상세의 배당·실적

- 숫자 영역 다음, 뉴스 앞에 기존 상세 화면 흐름으로 배치한다. 카드 밖 18px 제목, 카드 안 15px 값·12px 날짜/단위·16px 안쪽 여백을 공통 토큰으로 사용한다. PC는 가능한 경우 두 열, 390px은 한 열이며 같은 내용을 요약 카드에 중복하지 않는다.
- 주당 배당과 개인 예상 수령액을 섞지 않는다. 실제/예상 실적과 확인일·출처를 짧게 구분하고 미확인을 0으로 바꾸지 않는다. 이전 자료를 유지하는 갱신 실패 안내는 해당 목록 안 한 줄만 둔다. 미설정 개발 기능은 공개 화면에 빈 패널을 만들지 않는다. [공급·표시 명세](./PRODUCT_SPEC.md#기업-자료-공급-전환)를 따른다.

### 포트폴리오 선택과 관리

- 보유자산·거래 제목 곁에 같은 작은 선택란과 `관리`를 둔다. 생성·이름 변경·삭제는 하나의 대화상자에서 처리하며 통계 카드·별도 관리 메뉴를 추가하지 않는다. `전체`는 조회용이며 거래 입력의 대상 선택에는 제공하지 않는다.
- 이름은 최대 40자다. 선택란은 PC에서 208px 이내, 좁은 화면에서는 가용 폭으로 줄이고 긴 이름을 말줄임한다. 관리 목록은 이름 줄바꿈을 허용하며 버튼을 밀어내 가로 넘침을 만들지 않는다. 조작 높이는 44px 이상이고 input/body/label·surface/line/ink 토큰을 재사용한다.
- 거래가 있는 삭제창은 이동할 대상을 먼저 고르게 하며 `옮기고 삭제`로 결과를 분명히 한다. 마지막 포트폴리오는 삭제하지 못한다. 저장 중 중복 제출을 막고 오류는 같은 창에 표시한다. 대화상자는 이름·초점 제한·Esc 닫기·닫은 뒤 관리 버튼 복귀를 제공한다.
- 조회 전환만으로 거래 대상이나 다른 계정의 편집 내용을 이어 붙이지 않는다. 계산과 복구 범위는 [여러 포트폴리오 명세](./PRODUCT_SPEC.md#여러-포트폴리오)를 따른다. 참고: [Sharesight 합산 화면](https://help.sharesight.com/au/consolidated_views/)도 합산 조회와 개별 포트폴리오 추가를 구분한다(2026-09-24 확인).

### 보유자산 세부 다듬기 검토

**현재 배치 기준:** 현재 요약과 기간 성과를 하나의 `portfolio-overview` 흰 카드에 위아래로 연결하고, 그 아래 한 카드에서 보유종목 / 투자 성과 두 탭을 전환한다. 보유종목을 기본 선택하고 탭 줄은 같은 위치에 유지한다. 비활성 패널은 숨기되 보유 표의 검색·시장 필터·정렬·추가 표시와 성과의 페이지·월/년 선택 상태를 보존한다. 내용 안의 중복 제목·종목 수·카드 테두리는 생략한다. 탭은 종목 수 없이 이름만 표시한다. 선택은 둥근 옅은 회색 배경·진한 글자로 구분하고 검은 밑줄을 겹치지 않는다. 클릭에는 강제 초점 이동을 넣지 않으며 키보드 초점 테두리는 유지한다. 탭은 방향키·Home/End와 44px 이상 터치 영역을 제공하며 설명 펼침·접기는 추가하지 않는다. 별도 성과 메뉴·보유자산 제목 옆 이동 버튼·눈에 보이는 ‘기간 성과’ 큰 제목은 두지 않으며, 영역의 접근성 이름과 `id`는 기존 성과 주소 연결을 위해 유지한다. 관심종목·투자 노트·거래내역은 유지한다. 기능·계산은 [보유자산 명세](./PRODUCT_SPEC.md#보유자산-개선-검토), 구현·검증 상태는 [진행판 T12](./PROJECT_STATUS.md#작업별-진행판)를 따른다. 자동 배당 안내는 아래 4번의 개발 화면 한정이며 출시 제공은 보류한다.

| 순서 | 배치와 역할 |
| --- | --- |
| 1. 핵심 요약 | 총 보유자산을 가장 크게 두고 평가손익·원가를 아래에, 오늘 손익 제목·합계와 작은 구성 줄을 카드 오른쪽 절반의 묶음에 둔다. 모바일은 총자산→평가손익·원가→오늘 손익 순서로 읽는다. 세 칸이나 구분선으로 나누지 않는다. |
| 2. 기간 성과 | 같은 요약 카드 아래에 기간·날짜·차트 종류 → 손익·단위 → 차트를 둔다. 세부 규칙은 [기간 성과 화면 기준](#기간-성과-화면-기준)에서 작업별로 찾는다. |
| 3. 상세 영역 · 보유종목 탭(기본) | 그래프 바로 다음의 개별 종목 확인·관리 영역. 탭은 이름만 표시한다. 상단은 작은 도넛·설명과 검색·필터 도구줄, 아래는 전체 폭의 종목표로 구성한다. 좁은 화면은 도넛 → 도구줄 → 표 순서다. 도넛·표 위에 제목·종목 수·중첩 카드를 반복하지 않는다. 종목·평가액·평가손익·오늘 손익·비중을 비교하며 수량·평균단가·수익률은 관련 금액의 보조 줄에 둔다. 검색·시장 선택·정렬·거래 기록/CSV 진입은 기존 도구 영역에 모으고 수정·삭제를 유지한다. 기본 ‘비중 높은 순’으로 처음 20개를 보여주고 표 아래 ‘20개 더 보기’로 누적 표시한다. 마지막 묶음은 실제 남은 개수를 안내하며 전부 표시하면 버튼을 없앤다. 20개를 초과하는 검색/필터 결과에는 ‘현재 표시 / 결과 전체 종목 수’를 함께 둔다. 구성 전용 목록·검색·페이지 이동은 추가하지 않는다. |
| 3. 상세 영역 · 투자 성과 탭 | 같은 상세 카드에서 전체 투자 기간의 월별·연도별 성과 표를 제공한다. 그래프의 날짜 선택과 독립인 범위·월/년 선택을 유지하며 상세 기준은 [월별·연도별 표](#월별연도별-투자-성과-표)를 따른다. |
| 4. 예상 배당 | 보유목록 다음에 발표 자료로 계산한 배당 3건을 작은 행으로 보여준다. 국내·해외 주식·ETF 자료를 같은 화면 기준으로 제공하되 현재 개발 화면 한정이며 운영 제공은 보류한다. 다가오는 지급 일정은 가까운 순, 지난 일정은 최근 순, 지급일 미확인은 마지막에 둔다. 종목·공시 원문·발표 지급일·세전·예상 세후·확인된 세율을 제공한다. 지급일 누락은 ‘지급일 미확인’, 세후 조건 누락은 ‘세율 확인 필요’로 구분하고 지난 지급일을 실제 입금 완료로 표시하지 않는다. 자료가 없는 종목·일부 공시만 확인한 종목·확인 기간에 발표가 없는 종목을 각각 짧게 알리며 전체 배당 합계처럼 표시하지 않는다. 연도는 자료 범위이고 오래된 확인 시각을 숨기지 않는다. PC는 종목과 금액 열을 정렬하고 390px에서는 종목 아래 두 금액을 나란히 둔다. 글꼴·색·간격은 공통 토큰을 사용한다. 별도 대형 카드·새 탭·설명 접기를 추가하지 않고 더 있는 행은 목록 확장으로 연결한다. 선택 포트폴리오에 거래가 없으면 영역을 생략한다. 해외 날짜 경계는 승인된 한국 날짜 마감 수량으로 추정하고 적용 시 기존 안내 줄에 한 번 알린다. 원문 링크는 키보드 초점과 구체적인 접근성 이름을 제공한다. |

- **전체 구성:** 도넛은 표 위의 짧은 요약 줄에 둔다. 112px 도넛과 종목 설명을 가로로 묶으며, 영역 폭 1,000px 이상에서는 그 오른쪽에 검색·시장·정렬·CSV를 한 묶음으로 배치한다. 그보다 좁으면 도넛 다음 줄에 도구를 배치한다. 표는 항상 아래에서 카드 전체 폭을 사용하며 도넛 전용 세로 열을 예약하지 않는다. 모바일 표의 기존 2열 재배치는 표 자체 폭을 기준으로 유지한다. 별도 구성 카드·범례·종목 목록·검색·페이지 이동을 만들지 않는다.
- **도넛 중앙과 설명:** 기본은 최대 비중 종목의 비중을 중앙에, 이름·티커를 가까운 설명에 표시한다. 표 행의 마우스 이동·키보드 초점에서는 해당 종목으로 바뀐다. 이름은 최대 두 줄이며 전체 이름은 표에서 확인한다. 중앙 수치는 PC·모바일 모두 section 18px로 표시하고 전체 합계·종목 수는 반복하지 않는다. 분모 설명은 caption 12px ‘전체 보유자산 기준’ 한 번만 둔다.
- **비중과 색:** 모든 종목의 실제 비중을 원주 길이에 그대로 반영하며 작은 조각을 늘리거나 ‘나머지’로 묶지 않는다. 간격·둥근 끝·3D·그림자로 비중을 왜곡하지 않는다. chart·comparison-8·comparison-11·comparison-4·muted·focus 6색을 종목 ID 기준으로 안정적으로 반복한다. 표는 비중 숫자와 작은 원형 색 표식만 두며 추가 막대는 없다. 색만으로 개별 종목을 식별하게 하지 않는다.
- **연결과 접근성:** 표의 마우스 이동·키보드 초점으로 해당 원호의 ink 테두리, 주변 흐림, 표 비중 밑줄, 이름·중앙 비중을 함께 갱신한다. 정확한 종목명·금액·비중은 항상 기존 표에서 읽는다. 도넛 조각에는 버튼·탭 정지점·필수 클릭/hover·새 툴팁을 추가하지 않는다. 스크린리더에는 구성 그래프의 이름과 전체 분모, 확인 중인 종목과 비중을 제공하되 매 마우스 이동을 실시간 낭독하지 않는다.
- **누락·빈 상태·많은 종목:** 일부 평가액 누락 시 도넛을 만들지 않고 짧은 상태 안내와 표 비중 —를 표시한다. 빈 보유목록에는 구성 영역이 없으며 검색 결과가 없어도 실제 보유자산이 있으면 전체 구성은 유지한다. 실제 0원 종목의 원호는 길이 0이고 가짜 양수 조각을 만들지 않는다. 비중 숫자는 소수점 첫째 자리로 반올림하고 결과가 0이면 `0%`로 표시한다. 작은 종목과 200/2,000개도 전체 분모·원호에 포함하지만 미세 원호의 개별 식별 가능성은 보장하지 않는다. 실제 화면·대용량 성능 검증 여부는 진행판의 근거로 구분한다.
- 플러스 빨강·마이너스 파랑·0 중립을 유지하고 색만으로 부호를 전달하지 않는다. 총액은 중립색으로 둔다. 상시 설명은 판단에 필요한 짧은 라벨에 한정하며 계산 과정·세율 예외를 화면이나 도움말에 길게 옮기지 않는다. 배당은 ‘예상’임을 표시하고 누락 자료를 0으로 보이지 않게 한다.
- 첫 화면에서 현재 수치와 투자 흐름을 함께 읽도록 요약·그래프 주변 여백을 줄인다. 보유종목에는 작은 구성 도넛과 기존 표를 통합하고 월별 표는 별도 투자 성과 탭에서 전환한다. 큰 구성 카드·중복 종목목록을 세로로 쌓지 않는다. 그래프는 실제 관측점·축·툴팁을 보존하며 높이와 중복 제목·큰 성과 수치·별도 카드 간격을 줄인다. 금액 첫 줄을 같은 높이로 정렬하고 긴 종목명·반복 수정/삭제 아이콘의 비중을 낮춘다. 모바일에서는 핵심 숫자와 행을 재배치하며 숫자/글자 일괄 축소나 정보 삭제로 맞추지 않는다. 동일 순서 안에서 좁은 화면에 필요한 줄바꿈을 허용한다.
- 종목명은 [시세 명세의 영문 정식명 기준](./PRODUCT_SPEC.md#시세와-목표가)을 공통 적용한다. 티커를 함께 표시해 같은 기업의 다른 상장을 구분한다. 구성용 종목명 목록을 중복해서 만들지 않으며 이름과 티커는 기존 보유종목 표에서 확인한다.

보유자산 배치 참고(2026-09-22 확인): [Robinhood Investing](https://robinhood.com/us/en/support/articles/using-charts/)의 첫 투자 화면 전체 포트폴리오 차트, [Fidelity 모바일 Home](https://www.fidelity.com/mobile/fidelity-investments-app)의 잔액·잔액 차트와 빠른 보유종목 확인, [Sharesight Portfolio Overview](https://help.sharesight.com/show_portfolio/)(공식 도움말 2026-09-16 갱신)의 요약 → 그래프 → 투자 종목 표 순서를 참고한다. 공식 안내로 확인한 흐름이며 Fidelity 화면의 정확한 크기·위치는 확인하지 않았다. 이용 규모·앱 전체 평판은 아래 2026-09-21 조사 기준과 한계를 유지한다. 그 인기나 평점이 특정 배치의 우월성을 입증하지는 않는다. Centbloom에는 현재 상태 → 기간 흐름 → 보유종목 중심의 상세 보기 순서를 적용하되, 현금 포함 여부·수치 이름·계산을 타사에서 가져오지 않는다. Sharesight의 투자 화면과 별도 보고서 구분, [Trading 212 Portfolio Charts](https://helpcentre.trading212.com/hc/en-us/articles/22435640547613-Portfolio-Charts)의 동일 영역 보기 전환을 참고한다. 그래프는 상시 표시하고 보유종목·투자 성과 두 탭에서 상세 내용을 전환하며 구성은 보유 표 위의 작은 도넛으로 통합한다. 이것이 실제 사용자에게 최적이라는 사용성 실험 결과는 아직 없다.

구성 도넛 참고(2026-09-22 공식 자료 확인): [Sharesight Diversity Report](https://help.sharesight.com/au/diversity_report/)는 원형 구성 그래프와 아래 상세 종목표를 제공하며 [공식 화면 예시](https://www.sharesight.com/blog/calculate-investment-portfolio-diversification-sharesight/)도 공개한다. [Navexa Diversification](https://help.navexa.com/en/articles/12927106-portfolio-diversification-report-bar-pie-charts)은 원형/막대 전환과 상세표를 제공한다. [Trading 212 Pies](https://www.trading212.com/auto-invest)의 자동 투자·목표 배분 기능은 실제 보유 비중 조회와 다르므로 도입하지 않는다. 공식 도움말·공개 화면 자료를 통한 확인이며 로그인 앱을 직접 조작한 검증은 아니다. 원형 전체 구성 + 정확한 표라는 역할만 채택하고 112px 도넛·상단 요약과 전체 폭 표 배치는 Centbloom의 설계 판단이다. [W3C 색 이외 정보 원칙](https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html)에 맞춰 이름·숫자·테두리·밑줄을 병행한다. 유명 서비스의 채택이나 코드 검사만으로 이 배치의 사용성·전체 접근성 검증을 완료했다고 보지 않는다.

기간 성과 조작·차트 참고(2026-09-21 확인): [IBM Carbon 날짜 입력](https://carbondesignsystem.com/components/date-picker/usage/)의 입력칸에서 달력 열기/직접 입력, [Carbon 축·눈금](https://carbondesignsystem.com/data-visualization/axes-and-labels/)의 간단한 라벨·결측 구분, [TradingView 통화 표시](https://www.tradingview.com/blog/en/currency-on-your-chart-17337/)의 축 단위 구분을 참고한다. 외형을 복제하거나 이용 효과가 입증됐다고 주장하지 않으며 Centbloom 공통 글꼴·색·여백을 우선한다.

비교 차트 참고(2026-09-22 확인): [TradingView 공식 Compare 도움말](https://www.tradingview.com/support/solutions/43000543053-how-to-use-the-compare-tool/)에 연결된 [예제](https://www.tradingview.com/embed/a2iEQwa2/)의 서로 다른 색 실선·끝값 표기, [Koyfin 차트 업데이트](https://www.koyfin.com/help/release-notes/v3-27-chart-updates/)의 월/연도 중심 달력 눈금과 원본 곡선을 참고했다. TradingView 예제는 2013년 작성, Koyfin 자료는 2024년 공개 화면이므로 2026년 전체 최신 기본 UI로 일반화하지 않는다. 한 줄 날짜축·항상 보이는 강조/제거 목록은 Centbloom의 가용 폭과 조작에 맞춘 설계이며 타사의 동일 기능이나 효과 입증으로 주장하지 않는다. 날짜축은 [Datawrapper 공식 안내](https://www.datawrapper.de/academy/customizing-your-line-chart)(2026-09-22 확인)의 날짜 형식·눈금·격자 분리 방식을 참고하되 36px 고정이나 특정 형식의 우월성을 입증한 자료로 취급하지 않는다. 계산 방법은 가져오지 않고 기존 성과 의미를 유지한다.


### 기간 성과 화면 기준

화면의 수치 의미는 [제품 명세](./PRODUCT_SPEC.md#자산통화성과의-의미), 조사 근거는 [서비스 비교](#기간-성과-수치의-서비스-비교)에 둔다. 필요한 조작이나 표시 항목만 읽는다.

[배치](#기간-성과-배치) · [날짜 조작](#기간-성과-날짜-조작) · [손익 요약](#기간-성과-손익-요약) · [차트 선](#기간-성과-차트-선과-값) · [축·단위](#기간-성과-차트-축과-단위) · [비교 목록](#기간-성과-비교-목록) · [모바일·크기](#기간-성과-모바일과-크기) · [결측·실패](#기간-성과-결측과-실패)

#### 기간 성과 배치

- 현재 요약과 같은 흰 카드의 바로 아래에서 기간 버튼·항상 보이는 시작/종료일·차트 종류 한 도구줄 → 기간 손익·단위 한 줄 → 보유자산 추이/수익률 비교 차트 순서로 읽는다. 독립 카드 테두리·중복 제목·큰 위아래 여백은 없애고 요약과 기간 영역 사이의 얇은 구분선만 둔다.
- 기간과 차트 종류 버튼은 같은 8px 모서리·옅은 회색 선택 배경·진한 글자로 통일하고 밑줄이나 그룹 전체 회색 바탕을 겹치지 않는다. 상단 차트 종류 버튼은 두 모드에서 같은 오른쪽 자리를 유지한다. 두 모드를 바꿔도 위의 기간 손익 금액과 조회 기간은 유지한다.
- 기존 `/insights` 주소는 이 영역으로 이어 주며, 눈에 보이는 제목을 없애도 영역의 `aria-label`·`id`를 유지한다.

#### 기간 성과 날짜 조작

- 기간 선택은 ‘1일·5일·1개월·3개월·올해·1년·전체’ 7개를 두고 폭이 부족하면 터치 영역을 줄이지 않고 줄바꿈한다.
- 날짜 두 개는 얇은 공통 테두리 안에서 대시로 연결하고, 같은 폭의 각 입력 안에 숫자를 중앙 정렬한다.
- 왼쪽 달력 버튼은 시작일→종료일 순서로 고르는 기간 선택을 연다. 시작일은 전체 조회 범위에서 고를 수 있고 종료일은 새 시작일부터 고른다. 두 날짜를 모두 고른 뒤 함께 반영하며 중간 취소는 기존 기간을 유지한다.
- 시작일/종료일 입력칸을 직접 누르면 해당 날짜 하나만 바꾸고 닫는다. 기간 선택 도중 입력칸 클릭·키보드 직접 편집은 임시 기간을 취소하고 단일 편집으로 전환한다.
- 같은 글꼴·surface·line·radius-card와 진한 선택 날짜/옅은 기간 배경을 쓰고 연도/월·이전/다음·닫기만 제공한다. 장식 아이콘은 접근성 읽기에서 제외하고 시작/종료 라벨은 접근성 이름으로 유지한다.
- 직접 입력은 YYYY.MM.DD·YYYY-MM-DD·숫자 8자리를 지원한다. 아래 방향키로 날짜 격자에 들어가고 방향키·Home/End·PageUp/Down으로 이동한다. 선택/Escape 뒤 단일 편집은 해당 입력칸, 기간 선택은 달력 버튼으로 복귀하며 바깥 클릭·초점 이탈로 닫는다.
- 조회 범위 밖 날짜와 잘못된 날짜는 반영하지 않는다. 단일 날짜 변경과 완료된 기간 선택은 즉시 반영하고 직접 선택 펼침·접기와 닫기용 적용 버튼은 두지 않는다.

#### 기간 성과 손익 요약

- ‘기간 손익’ 라벨은 label 13px, 원화 기준은 caption 12px로 한 번 표시하고 손익 금액은 body 15px로 표시하고 일별 연결 수익률은 금액 옆에 붙이지 않고 수익률 비교 차트와 목록에 둔다. 이 작은 요약은 기간 조작부 아래에서 라벨·원화 기준·금액을 한 줄로 모으고, 오른쪽에는 차트 단위를 둔다. 현재 요약의 대표 금액보다 작게 표시해 현재 상태와 선택 기간의 성과를 구분한다.
- ‘운용 수익률’·‘내 자금 수익률’ 별도 카드와 구분 설명·세로 구분선을 제거한다. 기간 손익은 해당 기간에 번 금액이며, 내 수익률은 매매 투입·회수를 제외한 일별 연결 성적이다. 서로 다른 지표를 같은 금액/%처럼 묶지 않는다.
- 마감 보유분과 계산 대상 기간의 매매가 모두 없는 기간은 ‘미운용’, 수익률을 계산할 수 없으면 금액은 유지하고 비교 목록의 내 수익률에 ‘수익률 계산 불가’를 표시한다. 빨강/파랑/0 중립·공통 모서리/글꼴을 유지한다.
- 기록 나이·일별 성과 소개·ECB 공급/환산 과정·통화별 환율 기준일·계산 도움말·시작/종료 평가액 화살표·선택 기간과 무관한 현재 보유분 손익은 제거한다. 환율 출처·기준일 데이터와 계산 의미는 명세에 보존하며 설명창으로 옮기지 않는다.

#### 기간 성과 차트 선과 값

- 보유자산 추이는 녹색 실선 한 개와 18%→6%→0%로 사라지는 옅은 영역으로 표시한다. 마지막 날짜의 실제 유효값에만 흰 테두리의 작은 녹색 점을 둔다(0원 포함). 끝값 누락 시 이전 점을 마지막 값처럼 표시하지 않는다.
- 선택 버튼과 중복되는 단일 선 범례는 생략하고 미보유 기간 범례는 유지한다. 수익률 비교는 내 수익률 녹색과 비교 전용 12색의 같은 굵기 실선을 사용한다. 0% 기준선만 유지하고 반복 격자는 없앤다. 넓은 차트(760px 이상)에서 비교 대상이 3개 이하이면 선 끝에 티커·수익률을 겹치지 않게 직접 표시한다.
- 원본 관측점과 툴팁의 정확한 날짜/금액은 보존하며 없는 비교값을 이어 그리지 않는다. 계좌 입금액으로 오해할 수 있는 누적 순투입금 선·범례는 표시하지 않으며 숨긴 값은 금액축 범위에도 포함하지 않는다.

#### 기간 성과 차트 축과 단위

- 금액·수익률축은 왼쪽에 반올림 간격과 천 단위 쉼표로 표시하며 원·만원·억원 또는 USD 배율 단위를 위에 한 번 둔다. 보유자산 금액축은 선택 기간의 유효한 최저·최고 금액에 8% 여백과 읽기 쉬운 눈금 간격을 적용하고 0을 강제하지 않는다. 실제 0원은 포함하며 같은 금액만 있는 기간은 1% 또는 최소 화폐 단위의 여백을 둔다.
- 금액 글자 길이에 따라 왼쪽 축 폭을 확보하고 날짜 양 끝 라벨만 안쪽으로 맞춰 잘림을 막는다. 수익률축의 0% 기준과 원본 금액·날짜 좌표는 유지한다.
- 날짜 위치는 실제 UTC 날짜 간격을 유지하고 시작/종료일을 규칙 눈금에 억지로 끼워 넣지 않는다. 날짜축은 두 차트·모든 기간에서 높이 36px·한 줄·동일 caption 글꼴/색/기준선으로 통일한다. 별도 연도 띠와 날짜용 가로·세로 구분선은 두지 않는다.
- 1일은 ‘09:30’ 시각, 5일은 ‘9월 22일’ 날짜와 툴팁의 정확한 시각을 사용한다. 단위 옆에는 1분/30분 간격·KST를 짧게 표시한다.
- 모든 기간의 단위·간격 표시는 같은 `performance-chart-meta` 영역과 `performance-currency` 스타일을 사용한다. 공통 UI 글꼴·caption 12px·muted 색·label 행간을 요소에 직접 적용해 부모 위치에 따라 기본 15px 검정으로 바뀌지 않게 한다. 기간 손익은 기존 공통 요약, 축·툴팁은 같은 차트 컴포넌트를 유지한다.
- 일별 차트의 45일 이내는 ‘9월 22일’, 그보다 길고 4년 이내는 ‘2026년 9월’, 4년 초과는 ‘2026년’ 형식이다. 짧은 구간이 연말을 넘으면 같은 줄에 연도를 함께 붙인다.
- 좌우 여백·Y축을 뺀 실제 plot 폭에서 양 끝 라벨의 안쪽 보정까지 고려해 라벨 사이 최소 24px, 최대 6개를 목표로 달력 간격을 선택한다. 글자를 축소하거나 줄바꿈하지 않고 필요한 날짜 눈금 수만 줄인다. 글자 폭은 보수적으로 추산하며 실제 기기·글꼴에서의 가독성 검증과 구분한다. 1·2·3일/주, 월·분기·반기·연도 등 실제 달력 기준 눈금을 사용하고 정확한 범위는 상단 시작/종료일을 유지한다.
- 일별 USD 자산 추이는 ‘현재 환율 환산’, 시간별 USD는 당시 환율을 사용하므로 해당 문구 없이 표시하며, 수익률 차트의 범례·툴팁은 ‘내 수익률’과 ‘종목 참고 수익률’로 구분한다.

#### 기간 성과 비교 목록

- 비교 검색은 그래프 아래 독립 영역의 오른쪽에 두고 왼쪽에는 낮고 평평한 비교 목록을 둔다.
- 아래 비교 목록은 항상 보이며 종목 선택/키보드 조작으로 해당 선만 진하게 강조하고 다시 누르면 해제한다. 나머지 선은 옅어지지만 사라지지 않고 축 범위도 유지한다. 종목 제거와 강조 버튼은 분리하며 삭제 후 다른 종목의 색을 바꾸지 않는다.
- 검색으로 최대 12개 종목을 추가하고 목록에서 개별 제거·조회 실패 재시도를 제공한다.
- 목록은 색 선·티커(내 수익률)·수익률·개별 삭제를 한 줄로 모아 필요한 폭만 쓰고 항목 단위로 줄바꿈한다. 개별 카드 테두리·균등한 큰 빈 열·별도 KRW 행·긴 회사명 반복은 두지 않는다. 정식명은 검색 결과와 접근성 설명에 유지하고 배당/실패 기준만 짧은 보조 줄로 표시한다. 강조/제거는 별도 키보드 조작과 터치 44px 영역을 유지하며 설명의 펼침·접기는 두지 않는다.
- 원화 기준은 내 수익률에 한정하고 비교 종목은 목록 아래 공통 안내 한 줄에 현지 통화·내 매매 미반영을 표시하고 종목별 배당 포함/가격 기준은 목록의 작은 보조 글자로 구분한다. ‘배당 포함’은 실제 유효한 조정종가를 사용하는 경우에만 표시하며 자료 실패·미보유 기간은 짧게 표시한다.

#### 기간 성과 모바일과 크기

- PC 버튼과 날짜 묶음은 36px 높이, 터치 조작부는 44px 이상으로 맞춘다. PC에서 기간과 날짜를 양 끝으로 벌리지 않고 같은 높이의 가까운 한 줄로 정렬하며, 좁으면 날짜 묶음과 차트 종류 묶음을 순서대로 줄바꿈한다. 밀도가 높은 이 기간 조작부의 날짜 글자는 PC에서 기간 버튼과 같은 label 13px로 두고, 터치 입력은 input 16px를 유지한다. 날짜 묶음 안의 입력은 PC 32px, 터치 또는 화면 600px 이하에서 44px 최소 높이를 제공하며 hover/focus는 공통 테두리와 옅은 배경으로 구분한다.
- 좁으면 묶음 단위로 자연스럽게 줄바꿈한다. 작은 화면도 기간 버튼은 같은 순서, 날짜 두 개는 한 묶음, 기간 손익 금액과 비교 목록의 수익률은 각 위치를 유지한다.
- 내부 폭 720px 이하에서는 검색을 위, 목록을 아래에 두며 항목 추가/제거로 검색 위치를 바꾸지 않는다.
- 그래프 높이는 PC 280px·모바일 240px로 두고, 주변 중복 제목·테두리·여백을 줄여 요약과 그래프의 전체 높이를 절제한다.

#### 기간 성과 결측과 실패

- 매매시각 미기록·시세 누락 구간을 비우는 현재 시간별 화면은 출시 가능한 최종 실패 처리로 승인된 것이 아니다. 없는 값을 임의로 잇지 않는 정확성 조건은 유지하되, 복구·대체 간격·필수 입력 보완을 먼저 검토하고 사용자와 결정한다. 대안과 결정 대기는 [시간별 명세](./PRODUCT_SPEC.md#1일5일-시간별-자산-그래프)에, 검증·출시 보류는 진행판 T12에 둔다. 최종 오류 화면은 원인과 실제 가능한 다음 행동을 짧게 제공하고 설명을 여러 줄 반복해 그래프를 밀어내지 않는다. 현재 안내만으로 완료 처리하지 않는다.
- **기간 성과 갱신 실패:** 같은 입력·같은 조회 기간의 정상 결과를 유지할 때 `갱신하지 못해 이전 결과를 표시합니다.` 한 줄만 기존 notice·보조 글자 스타일로 표시한다. 정상 복구 때 해제하며 상시 배지·설명 펼침은 추가하지 않는다. 최초 실패/다른 기간에서는 가짜 그래프·0을 만들지 않고 실패한 종목/환율 조회를 짧게 안내한다. 계산 후 저장 경고는 조회 실패 안내와 구분한다. 유지 조건·재시도 횟수는 [성과 명세](./PRODUCT_SPEC.md#자산통화성과의-의미)에 둔다.

### 기간 성과 수치의 서비스 비교

확인일: 2026-09-21. 아래는 공식 도움말·교육 자료에 명시된 제공 방식이며 로그인 계정의 최신 화면을 직접 조작한 결과가 아니다. 앱 전체 평판과 해당 기능의 사용성은 구분한다. 기능 변경 전 조사·보고 절차는 [작업 원칙](./AGENTS.md#작업응답-원칙)을 따른다.

| 서비스·화면 | 실제 제공하는 수치·구분 | 참고 범위와 출처 |
| --- | --- | --- |
| Robinhood 일반 Investing | 포트폴리오 차트에 오늘의 손익 금액(USD)과 수익률(%)을 함께 제공한다. 외부 입출금/자산 이전을 수익에서 제외하며 Modified Dietz의 영향을 받은 계산이라고 설명한다. | 기본 투자 화면에서 금액과 %를 함께 제공하는 근거다. 별도 관리형 Strategies 상품의 단순 수익률/시장 비교 방식을 일반 계좌와 섞지 않는다. [공식 Using charts](https://robinhood.com/us/en/support/articles/using-charts/) |
| Trading 212 Portfolio | 현금 포함 설정으로 보유분 평가손익 화면과 투자 금액·시점을 반영하는 MWRR 화면을 전환한다. 후자는 계좌 가치·총 손익 금액·수익률·선택적 순입금액을 제공한다. | 최초 기본 선택은 문서에 명시되지 않았다. 계좌 전체 성과에는 현금·배당·이자 등도 포함돼 현금 장부가 없는 Centbloom과 계산 범위가 다르다. 국가/계좌별 예외를 일반화하지 않는다. [공식 Portfolio Charts](https://helpcentre.trading212.com/hc/en-us/articles/22435640547613-Portfolio-Charts) |
| Sharesight Performance Report | 선택 기간의 성과를 금액 또는 %로 전환하고, 주가 손익·배당·환율 손익으로 나눈다. 현재 보유분만/매도분 포함 선택과 비교 기능을 제공한다. | 기본 계좌 홈이 아닌 전문 성과 보고서다. Modified Dietz를 사용하며 투자 기간 조건에 따라 기간 수익률과 연환산 수익률을 구분한다. 이를 Centbloom의 비연환산 수익률과 동일시하지 않는다. [공식 보고서](https://help.sharesight.com/performance_report/), [수익률 기간 기준](https://help.sharesight.com/nz/absolute-and-annualised-return/) |
| Fidelity 웹 Performance | 계좌 잔액·기간별 성과·시장지수 비교와 시간가중/금액가중 수익률을 구분해 제공한다. | 두 방식 모두 쓰이는 반대 사례다. 웹 상세 분석 근거이며 모바일 첫 화면의 기본 배치라고 주장하지 않는다. [2026년 공식 교육 자료 8~9쪽](https://media.fidelity.com/assets/Fidelity.com_VMS/683/955/022526_FidCom_PM_Transcript_FINAL.pdf) |

선정 근거와 한계:

- Robinhood는 [2026년 2분기 회사 발표](https://investors.robinhood.com/node/16381/pdf)의 공식 고객 지표(Funded Customers) 2,840만 명, [미국 App Store](https://apps.apple.com/us/app/robinhood-trading-investing/id938003185) 4.3/5·약 480만 평가를 확인했다. 전자는 앱 활성 이용자나 특정 기능 이용자 수가 아니다.
- Trading 212는 [공식 Press](https://www.trading212.com/press)의 누적 자금 입금 계좌 500만 이상(통계 기준일 미표기), [영국 App Store](https://apps.apple.com/gb/app/trading-212/id566325832) 4.7/5·약 40.1만 평가다. 계좌 수와 사람 수를 동일시하지 않는다.
- Fidelity는 [2025년 연차보고서 6쪽](https://about.fidelity.com/_files/ugd/163ca5_dfca73d35f224b419c0463d3226f347c.pdf)의 고객 약 5,700만 명, [미국 App Store](https://apps.apple.com/us/app/fidelity-investments/id348177453?platform=ipad) 4.8/5·약 330만 평가다. 고객 수는 앱 이용자 수와 다르다.
- Sharesight는 [공식 소개](https://www.sharesight.com/uk/reviews/)의 이용자 60만 이상, [Trustpilot](https://www.trustpilot.com/review/www.sharesight.com) 3.9/5·609개 리뷰다. 전문 포트폴리오 기능의 관련성이 높아 포함했으며 일관되게 높은 평판의 사례라고 포장하지 않는다.
- 토스증권은 [공식 소개](https://toss.im/service/securities)에서 2026년 7월 정상 계좌 가입자 1,000만 이상·MAU 630만을 확인했다. 그러나 공개 공식 자료에서 기간 성과 수치와 계산 방식을 확인하지 못해 위 계산 비교에서 제외한다. 토스 앱 전체 평점을 증권 기능의 평점으로 취급하지 않는다.

**적용 기준:** 기간 손익에는 번 금액만, 수익률 비교의 내 선·목록에는 매수·매도금 자체의 영향을 제외한 일별 연결 수익률을 제공한다. 투자 규모가 시점마다 다르면 금액과 연결 수익률의 부호가 다를 수 있으므로 같은 지표의 금액/%처럼 붙이지 않는다. 두 수익률 카드 및 추상적인 구분 문구는 제거한다. 위 서비스 조사는 제공 항목 참고이지 이 배치의 사용자 이해도가 검증됐다는 뜻은 아니다. 기간 선택·보유자산 추이·수익률 비교를 유지하며 별도 수익금 그래프·현금 장부·입출금 입력은 추가하지 않는다. 기간 손익과 내 수익률은 동일한 선택 기간·원화 기준으로 계산하되 의미를 구분하며, 외부 비교선은 현지 통화의 참고 수익률로 명시해 같은 매매를 재현한 성과와 구분한다. 현재 앱은 현금·배당을 포함한 전체 증권계좌가 아니므로 타사 수치 이름만 복제하지 않는다. 현금 잔액에는 별도 기록이 필요하다는 [Sharesight 현금 동기화 안내](https://help.sharesight.com/us/sync-historical-transactions-to-your-cash-account/)(2026-09-21 확인)를 참고하며 실계좌 연동 전까지 그 입력 범위를 추가하지 않는다. 상세 계산은 [성과 의미](./PRODUCT_SPEC.md#자산통화성과의-의미), 구현·검증·배포 상태는 [진행판](./PROJECT_STATUS.md#검증-기록)을 따른다.

### 월별·연도별 투자 성과 표

- 그래프 아래 상세 영역의 `투자 성과` 탭에서 제공한다. 탭 이름을 내용 제목으로 반복하지 않고 중첩 카드·장식·설명 펼침은 추가하지 않는다. 왼쪽의 `전체 기간 · 원화 기준`은 그래프 날짜 필터와 독립인 범위/통화만 알린다. 계산 의미는 [월별·연도별 명세](./PRODUCT_SPEC.md#월별연도별-투자-성과)에 둔다.
- 범위·통화 왼쪽, `월별 / 연도별` 선택 오른쪽을 유지한다. 기존 차트 종류의 작은 중립색 선택 컨트롤·글꼴·간격을 재사용한다. 월/년 전환과 페이지 이동으로 위의 차트·공통 조작부를 밀지 않는다. 좁으면 헤더만 자연스럽게 줄바꿈한다.
- 세 열 `기간 · 투자손익 · 수익률`만 제공한다. 기간은 왼쪽, 금액/%는 오른쪽 정렬하고 tabular-nums·본문 15px·표 머리글 13px를 사용한다. 값의 +빨강/−파랑/0중립을 유지하며 계산 불가와 미운용을 0%로 보이지 않게 한다. 반올림해 0이 되는 작은 값에는 부호·등락색을 붙이지 않는다.
- 최신 기간부터 6행씩 표시하고 아래 이전/다음 페이지 버튼으로 전체 이력을 확인한다. 월/년 전환은 최신 페이지로 이동한다. 숫자 페이지 안내만 짧게 제공하며 설명 접기·내부 스크롤·전체 10년 이력의 한 번에 나열은 하지 않는다. 첫 투자월·미완료 월/해는 실제 포함 날짜를 기간 아래 caption으로 표시한다. 모든 달에 날짜를 반복하지 않는다. 터치 버튼 44px·키보드 포커스를 유지하고 모바일에서도 세 열의 역할을 바꾸지 않는다.

참고 자료(2026-09-22 확인): [Sharesight Performance Report](https://help.sharesight.com/performance_report/)는 보유·매도분을 포함한 선택 기간 성과와 실현손익 보고서를 구분하며, [Multi-period report](https://help.sharesight.com/multi-period-report/)는 월·연도 등 여러 기간을 비교한다. [Portfolio Performance 계산 설명](https://help.portfolio-performance.info/en/reference/view/reports/performance/calculation/)과 [Dashboard](https://help.portfolio-performance.info/en/reference/view/reports/performance/dashboard/)도 기간 시작 평가액 기준의 성과·월/연도 비교를 제공한다. 월별 구분과 보유/매도분의 기간 변화 개념만 참고하고 타사의 연환산 방식·현금/배당 범위·요금제를 복제하지 않는다. 이 표의 배치가 인지도나 앱 평점으로 검증됐다고 주장하지 않는다. 별도의 Google Finance 시험 포트폴리오에서는 전량 매도 후 거래 기록 보존, 다른 보유종목이 있을 때 매도 종목의 과거 평가액이 그래프에 남는 것을 직접 확인했으며, 이를 모든 계산식이나 월별 성과 표의 검증으로 일반화하지 않는다.

## 공통 본문과 좌우 여백

일반 앱 화면은 공통 PageFrame을 사용한다. 사이드바를 제외한 공간에서 본문을 가운데 정렬하고 페이지별로 다른 최대 폭/바깥 여백을 추가하지 않는다. 폼·긴 글의 내부 읽기 폭은 필요에 따라 좁게 둘 수 있다. 인증 전용 화면과 모달은 해당 흐름을 유지한다. 주요 시장도 홈의 같은 본문 폭 안에 둔다.

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--cf-page-content-width` | `1120px` | 일반 페이지 본문 최대 폭 |
| `--cf-ad-rail-width` | `160px` | 로컬 사이드 레일 미리보기 폭 |
| `--cf-ad-rail-height` | `600px` | 로컬 사이드 레일 미리보기 높이 |
| `--cf-ad-rail-top` | `148px` | 상단 메뉴를 피하는 미리보기 고정 위치 |

데스크톱 바깥 여백은 space-6·상단 space-8. 본문 가용 공간이 1536px 이상이고 마우스 입력/화면 높이 760px 이상이면 오른쪽에만 레일과 space-6 간격을 둔다. 왼쪽은 광고 없는 여백으로 유지해 검색창과 본문의 중앙 정렬을 보존한다. 1536px은 본문 1120 + 오른쪽 레일과 대칭 여백 160×2 + 간격 24×2 + 바깥 여백 24×2의 합이다. 현재 홈 배치는 본문 배너 없이 오른쪽 스크롤 광고만 사용한다. 추가 광고 위치는 미정이며 확정 후 [광고 명세](./PRODUCT_SPEC.md#광고-배치)와 이 기준을 함께 갱신한다. 화면 전체 너비로만 판단해 사이드바/본문을 가리는 배치는 금지한다. 좁은 화면·터치 환경에는 사이드 광고를 숨기고 본문 폭을 확보한다. 모바일은 좌우 space-4·상단 space-6, 하단 탐색 높이를 확보한다. 광고 미송출/개인 화면에서도 같은 본문 정렬을 유지한다.

## 홈 섹션 통일

일반 페이지는 공통 검색·계정 헤더 → 본문 순서로 통일한다. 헤더는 상단에 스크롤 고정하며 PageFrame 본문 폭에 맞춘다. 전역 지수 띠와 별도 경로·도움말·통화 도구막대는 두지 않는다. 계정·알림은 아래의 [공통 검색과 계정 조작](#공통-검색과-계정-조작)을 따른다. 내 투자 탭은 검색 아래 본문 첫 행, 최근 검색은 홈 본문에만 둔다. 통화 관리는 기존 페이지/설정에서 제공하고 인증 전용 화면은 기존 구성을 유지한다. 주요 시장·종목 순위·주요뉴스·다가오는 일정·내 투자는 HomeSection의 같은 제목 행을 사용한다. 제목은 카드 바깥에 section 크기/semibold/ink, 제목과 카드 간격은 space-3이다. 제목 옆 번역 전환·KST도 같은 행에 둔다. 내용 카드는 surface 배경·line 테두리·radius-card와 조밀한 홈 공통 내부 여백 space-4를 사용한다. 순위의 세 하위 카드는 같은 표면 규칙을 적용하고 하위 제목은 body 크기로 구분한다. PC·모바일 모두 제목을 카드 안으로 옮기지 않는다.

거래량 상위·상승 종목·하락 종목 제목은 모두 전용 페이지 링크와 16px ChevronRight로 표시한다. 각 페이지에는 해당 순위 최대 10개와 주요뉴스를 위아래로 배치하고 시장 복귀 링크를 제공한다. 상세 상단의 공통 순위 링크는 선택 항목을 밑줄과 aria-current로 표시한다. 기업명을 주 정보, 티커를 보조 정보로 두고 모든 순위에 등락률을 표시한다. 상세는 현재가·등락률·거래량을 열로 정렬하고 관심 저장을 종목 링크 바깥의 44px 별 버튼/링크로 제공한다. 이 기존 별표를 개인화 선택으로 재사용하고 별도 ‘계속 보고받기’ 버튼을 만들지 않는다. 좁은 본문에서는 가격 아래 등락률, 티커 옆 거래량으로 재배치한다. 순위 변동은 보조 회색으로 유지해 가격 등락색과 구분한다. 로딩/실패 중에도 제목 링크를 유지한다. 주요뉴스 제목은 추가 항목이 있을 때 펼침/접기 버튼과 16px ChevronDown, aria-expanded/controls를 제공한다. 별도 더 보기 버튼은 두지 않는다. 모든 제목 조작은 같은 글꼴·색상·위치와 44px 터치 영역, 키보드 초점을 유지한다.

### 공통 검색과 계정 조작

공통 검색 오른쪽에 비회원은 로그인, 로그인 회원은 알림벨·원형 프로필 순서로 표시한다. 계정 확인 중에는 조작부 자리만 유지해 로그인 버튼이 잠깐 나타나지 않게 한다. 로그인은 흰 표면·line 테두리·radius-control의 작은 버튼 형태 링크다. 검색은 폭을 유연하게 쓰고 계정 조작은 크기를 유지한다. 390px에서도 같은 순서로 한 줄을 유지하며 검색 안내를 짧게 하되 실제 입력 label은 보존한다. 글꼴·색·간격은 공통 토큰, 버튼은 44px 이상 터치 영역과 focus-visible을 사용한다.

알림의 미읽음 표시는 확인된 새 항목이 있을 때만 붙이고 0·가짜 개수·깜빡임·자동 열기는 없다. PC에서는 최근 5개와 ‘전체 보기’를 담은 패널을 버튼 아래에 겹쳐 열어 본문을 밀지 않는다. 760px 이하에서는 같은 벨이 `/notifications`로 이동한다. 패널의 로딩·빈 목록·조회 실패는 구분하고 제목·시각·읽음 상태와 필요한 읽음 조작만 유지한다. 원문 연결은 전체 알림 목록에서 제공한다. 이 패널은 방문 요약의 본문 배치와 다른 기능이다.

프로필은 원형 계정 버튼이며 메뉴 안에 실제 계정 이름/이메일이 있을 때만 표시하고 계정 설정·로그아웃을 제공한다. 로그아웃 실패는 메뉴에 남겨 재시도할 수 있게 한다. 인증 미설정 로컬 모드는 사용자 모양의 설정 버튼만 제공하고 로그인된 것처럼 이름·알림을 만들지 않는다. PC 왼쪽의 설정/하단 프로필과 모바일 하단 설정은 반복하지 않으며 시장·내 투자 두 메뉴를 유지한다. 패널은 한 번에 하나만 연다. 키보드로 열면 첫 조작으로 초점을 옮기고 Esc로 닫으면 연 버튼에 돌려준다. 바깥 클릭·초점 이동으로 닫을 때는 사용자가 이동한 대상의 초점을 빼앗지 않는다.

**배치 근거(2026-09-23):** [토스증권](https://www.tossinvest.com/) 비회원 화면의 검색 옆 로그인, [Google Finance](https://www.google.com/finance/beta) 로그인 화면의 맨 오른쪽 프로필을 직접 확인했다. [Investing.com](https://www.investing.com/) 비회원 화면의 로그인/가입·부가 도구는 확인했지만 상단 홍보·도구 밀도는 따르지 않는다. [GitHub 공식 안내](https://docs.github.com/en/subscriptions-and-notifications/get-started/configuring-notifications)의 우상단 알림함·미읽음 표시를 참고한다. [TradingView Supercharts 안내](https://www.tradingview.com/support/solutions/43000746464-getting-started-with-supercharts/)의 가격 조건 Alerts와 활동 Notifications는 서로 다른 기능이며 Centbloom의 받은 소식함과 혼동하지 않는다. 토스·Investing의 로그인 후 배치는 미확인이고, 회사 인지도나 앱 평점을 이 배치의 우월성 근거로 삼지 않는다.

## 주요 시장

홈의 최근 검색 다음은 서로 높이를 강제하지 않는 두 흐름으로 구성한다. 1200px 이상 PC에서는 넓은 왼쪽에 ‘평소와 다른 움직임 → 종목 순위 → 주요뉴스’, 340px 오른쪽에 ‘주요 시장 → 다가오는 일정 → 장 시간표 → 내 투자’를 둔다. 열 사이와 각 열 안의 영역 간격은 space-6이다. 서로 다른 영역을 같은 높이의 행에 묶거나 남은 공간을 새 정보로 채우지 않는다. 좁은 화면은 ‘주요 시장 → 움직임 → 순위 → 일정 → 뉴스 → 시간표·내 투자’ 순서이며 조작 가능한 DOM도 이 읽기 순서를 따른다. 홈 순위는 PC·모바일 모두 거래량/상승/하락 중 한 목록을 표시하고 기존 순위 페이지로 연결한다. 탐색 화면의 기존 순위 배치는 유지한다. 시장 본문은 지역 선택 아래 두 지표이며, 중간 폭의 한 열 화면에서 본문이 860px 이상일 때만 선택·고지를 왼쪽, 지표를 오른쪽으로 배치한다.

카드 밖 ‘주요 시장’ 제목 오른쪽에 수동 `이전 · 1/3 · 다음`을 두고, 흰 표면 안에 `장중(최근 마감/대표) · 미국 · 아시아 · 유럽 · 기타` 선택을 한 줄로 둔다. PC·390px 모두 지표 두 개를 가로 한 줄로 표시한다. 지역을 누르면 같은 두 칸의 내용만 교체한다. 이름·가격·등락·시장 상태의 행 높이를 일정하게 유지해 로딩·실패·마지막 한 항목에서도 패널 높이가 바뀌지 않게 한다. 펼침·내부 스크롤·자동 넘김·등락률 정렬·아래 중복 목록은 없다.

Wanted Sans와 공통 토큰을 사용한다. 지수명·등락은 label, 가격은 section·semibold, 상태와 고지는 caption·muted다. 숫자는 동일 폭 숫자·부호·등락색을 유지하며 장중은 positive와 ‘장중’ 글자로 함께 구분한다. ‘시세 지연 가능’은 패널 아래에 한 번만 둔다. 개별 기준 시각·지연·VIX/금리 주의는 title/접근성 설명을 유지하며 이전 가격은 해당 값 곁에 표시한다. 지역·이전/다음은 44px 조작 영역, 선택 상태·비활성 경계·focus-visible·목록 연결을 제공한다. 일정 갱신 때 초점을 잃지 않도록 표시 슬롯 DOM을 재사용하고 재배치 애니메이션은 없다. [판단·데이터 기준](./PRODUCT_SPEC.md#시장-홈-주요-시장), [화면 조사](./PUBLIC_HOME_RESEARCH.md#주요-시장-묶음과-지수-목록)를 따른다.

## 구루와 앱 안 알림

구루 목록/상세는 기존 목록·상세 유형을 쓰고 공시 기준일·제출일·공개 시각 미확인·보고 범위 같은 해석에 필요한 차이만 유지한다. 운용사 이름을 제목으로 두며 인물을 개인 계좌의 주인으로 표현하지 않는다. 보고 수량 비교는 두 날짜의 숫자를 나란히 표시하고 미확인 티커/매입가를 추측해 채우지 않는다. 좁은 화면에서는 종목 아래 수량/금액/비중을 줄바꿈하며 작은 글자 축소로 맞추지 않는다.

- 주요 메뉴의 `구루 포트폴리오`에서 목록→상세로 이동한다. Stockcircle의 인물 카드·색·차트 구성을 복제하지 않고 기존 중립색 surface/line, ink/muted, 글꼴·간격 토큰을 유지한다.
- 목록은 검색·고정 위치의 인기순/규모순 조작 아래 작은 카드 그리드로 표시한다(넓은 화면 3열, 중간 2열, 390px 1열). 카드에는 운용사·관련 인물, 공시 금액/분기, 상위 3개 보유 종목과 비중, 보유 종목 수만 둔다. 사진·추정 수익률·순위 배지·큰 통계 카드를 추가하지 않는다. 금액의 정확한 값·제출일·원문은 상세에서 확인한다. 같은 발행사여도 다른 ETF·주식 종류·PUT/CALL을 합산하지 않으며 종목명에 구분을 유지한다. 동일 증권의 운용사별 분할 보고 행만 합산한다.
- 인기순은 사용자 선택에 따라 인지도·투자 철학의 대표성을 고려한 편집 선정이다. 접을 수 있는 선정 기준에서 실제 조회·저장 수 순위가 아님을 명시한다. 선정 다음은 공시 규모순, 동률은 이름순이며 자동으로 순서가 움직이지 않는다. 규모순은 13F 공개 보고액으로, 전체 운용자산이나 현재 평가액으로 부르지 않는다.
- 목록은 24개씩 추가하고 검색은 전체 연결된 목록에 적용한다. 연결 전 운용사는 별도의 접힌 상태 안내에만 표시하며 공시 금액 0이나 빈 포트폴리오 카드로 만들지 않는다. 상세는 운용사 제목/저장→분기 선택→공시 요약→보유 표 순서다.
- 종목 이름·금액·비중은 한 표에 한 번 표시한다. 이번/직전 보고 수량은 같은 행에서 비교하며 보고에서 빠진 행은 ‘이번 보고 없음’으로 표시한다. 증가·감소를 실제 매수·매도로 부르지 않는다. 분기와 정정본이 늘어도 선택부를 긴 버튼 행으로 펼치지 않는다.
- 대형 포트폴리오의 표는 전체 종목 대상 검색과 50행씩 더 보기를 제공한다. 목록 화면에 과거 모든 분기 자료를 전송하지 않고 미리보기 요약만 전달한다. 카드 전환·검색·정렬은 SEC 요청을 기다리지 않는다.
- 모바일은 표 전체를 가로로 밀지 않고 종목별 금액·비중·수량을 제목이 있는 작은 묶음으로 재배치한다. 키보드 초점 표시와 44px 조작 영역을 유지한다. 확인되지 않은 티커 경고를 모든 행에 반복하지 않는다.

앱 안 알림은 제목·원문·읽음 조작이 있는 단일 목록이다. 빈 상태·조회 실패·계정 미연결을 구분하며 0 통계/필터를 만들지 않는다. 읽음은 색뿐 아니라 문자로 표시한다. 공통 글꼴/색/간격 토큰, 44px 조작 영역과 focus-visible을 사용한다. 상시 진입은 [공통 검색과 계정 조작](#공통-검색과-계정-조작)을 따른다. 방문 요약은 같은 사건의 짧은 제목만 재사용하며 본문 위치·크기를 임의로 고정하지 않는다. [두 화면안](./PRODUCT_SPEC.md#방문-요약-화면안--선택-전)은 선택 전 제안이며 기존 홈/내 투자 본문 배치는 유지한다.

### 관심종목 가격 알림

- 기존 목표 매수가와 알림 구독은 구분한다. 종목 행의 작은 알림 설정 버튼에서 이하/이상 체크박스와 가격·통화를 편집하며 체크한 조건만 알림을 켠다. 켠 조건의 짧은 도달 상태만 행 아래 표시하고 새 요약 카드·홈 배너를 만들지 않는다.
- 편집은 기존 행 안의 옅은 배경, 두 조건 행, 한 개 저장/취소 영역으로 구성한다. PC·390px 모두 label/input/caption 토큰과 44px 이상 조작, 숫자 입력의 명시적 이름·체크 상태·초점 테두리를 유지한다. 색만으로 켜짐·도달을 구분하지 않는다.
- 도달 알림은 기존 벨/목록에 제목·실제 시세 기준 시각·읽음 조작을 표시한다. 같은 긴 설명을 다시 붙이지 않는다. 판정 주기·지연 상한·첫 관찰의 의미는 [가격 알림 명세](./PRODUCT_SPEC.md#가격-도달-인앱-알림)에 둔다. [TradingView 알림 조건](https://www.tradingview.com/support/solutions/43000763312-learn-how-to-configure-alerts/)의 조건·경계 통과·반복 구분을 참고했으며 화면을 복제하지 않는다(2026-09-24 확인).

## 변화 비교 카드

**경험 목표(D018):** 전문적으로 시장을 살피는 자기 이미지, 스스로 발견하는 감각, 이 공간에 속해 있다는 느낌을 정보 위계·밀도·절제된 말투·반복 방문의 연속성으로 만든다. 실제 역량 향상과 감정적 효과를 구분하고 객관적 정보 가치만으로 목표를 대체하지 않는다. ‘보고받는 느낌’은 내부 설계 비유이며 사용자에게 역할극 문구를 쓰지 않는다. 표시 데이터·성과·접근 권한은 사실대로 유지한다.

홈 검색 아래·기본 순위 위에 둔다. 선정·계산·유효기간·실패 처리는 [기능 명세](./PRODUCT_SPEC.md#평소와-다른-움직임)를 따르며 내부 선정 기준을 여는 버튼/설명창은 두지 않는다.

- **홈 요약:** 짧은 카드 최대 세 개를 한 열로 연결하며 홈에 필터/페이지 이동을 두지 않는다. 제목 옆 비회원 ‘로그인하고 전체 보기’·회원 ‘전체 보기’가 전용 목록으로 연결된다. 기업·티커·가격, 대표 변화 수치·당일 주가 등락, 짧은 관찰을 유지하며 기사·매체는 있을 때만 표시한다. 대표 수치는 section, 기업·관찰은 body/label, 보조 값은 caption이다. 좁은 카드에서는 숫자 묶음을 다음 줄로 옮기고 긴 이름은 링크의 전체 접근성 이름을 보존한 채 한 줄 말줄임한다.
- **상세 근거:** 카드의 ‘변화 근거’는 동일 종목 상세 `#market-movement`로 연결한다. 현재·기준 수치 두 줄과 최근 흐름은 상세에서 펼침 없이 보여주고 홈에 반복하지 않는다. 이번 값은 body/semibold, 기준은 label/regular·muted로 구분한다. 실제 기간·거래일·정규장/지연 조건을 유지하고 준비 중·실패/재시도·현재 변화 없음은 구분한다. 지난 이상 징후를 현재 사실처럼 유지하지 않는다.
- **회원 전체 목록:** `/movements`에는 제목·실제 거래일·미국 정규장 조건 아래 검색과 결과가 있는 종류 필터를 둔다. 결과 수/현재 범위와 이전·다음 버튼으로 20개씩 탐색하며 총 결과 개수는 제한하지 않는다. 내용 폭 760px 이상은 두 열, 그보다 좁으면 한 열이고 각 카드는 자기 폭에 맞춰 재배치한다. 390px에서는 검색을 한 줄 전체 폭으로 두고 필터만 자연스럽게 줄바꿈한다. 검색/필터 전환은 첫 페이지, 페이지 이동 후 결과 제목에 키보드 초점을 둔다. 버튼·별표·상세 연결은 44px 조작 영역과 보이는 초점을 제공한다. 아무 결과가 없는 화면에는 검색/필터·0개 통계를 반복하지 않고 검색 결과 없음은 ‘조건 지우기’로 구분한다.
- **최근 흐름:** 상세에서 ‘직전 5거래일 + 이번 장’의 6개 막대를 상시 표시한다. 실제 날짜·등락률 부호·같은 척도를 사용하고 이번 장만 중립 배경으로 구분한다. 툴팁 없이 값을 읽으며 가격 방향에만 시장 등락색을 적용한다. 비교 기간·거래일·정규장/지연 조건을 유지한다.
- **기사와 상태:** 기사가 있으면 제목·매체·발행일·원문 링크를 직접 보여주고 없으면 해당 영역을 생략한다. 수치 조건을 확인한 종목은 기사 없이도 같은 카드로 제공한다. 가상 직원/진행률·내부 수집 상태·기사를 원인으로 단정하는 표현은 쓰지 않는다. 유효한 정상 자료를 유지할 수 없는 실패에는 짧은 재시도를 제공한다.

관심종목이 있는 홈도 같은 카드 형식과 제목을 유지한다. 관심종목 변화와 공개 발견을 함께 배치하고 개인화 점수·선정 이유·사용자를 치켜세우는 문구를 붙이지 않는다. 기존 관심종목 미리보기에는 준비된 실제 뉴스와 이전 거래일의 변화가 있을 때 바로 표시하며, 과거 값의 실제 거래일을 남긴다. 사용자 숨김/덜 보기 조작은 제공하지 않는다. 승인된 회원 방문 비교는 실제 해당 계정의 방문 기준이 연결된 뒤에만 마지막 방문 이후 변화로 표현한다. 현재 저장된 과거 세션 값을 ‘지난 방문’으로 부르거나 변화가 약해진 종목에 새 이상 징후를 만들어 붙이지 않는다.

## 홈 일정 목록

홈은 ‘다가오는 일정’ 제목 옆 KST·전체 일정 링크와 흰 내용 표면을 사용한다. 가까운 예정 최대 3개와 최근 발표 최대 1개를 표시한다. 날짜/시각 → 발표 이름으로 읽고 얇은 구분선으로 나눈다. 일반적인 관찰 안내는 홈에 반복하지 않으며 항목을 누르면 발표 상세로 간다. 제목은 body/semibold, 시간은 caption이다. 실제/예상/이전 중 확인된 값·단위·기간과 최근 발표의 %p 차이는 유지한다. 물가 등의 높은 값/낮은 값을 주가 상승/하락색이나 좋음/나쁨으로 치환하지 않는다. 실제 관심 기업의 일정만 중립적인 관심종목 표시를 사용한다. 날짜 버튼·건수·주 이동·접기는 두지 않고 전체 일정/상세의 정보는 줄이지 않는다.

## 장 일정 시간표

다가오는 일정 바로 아래의 흰 접기/펼치기 영역. 기본은 장 시간표·미국 상태·다음 개장/마감(KST)·펼치기를 한 행에 표시하며, 영역이 좁으면 요약을 다음 줄로 보낸다. 버튼은 최소 44px 터치 영역과 키보드 초점·aria-expanded/controls를 제공한다. 펼치면 주요 6개국의 조밀한 가로 시간표를 표시한다. KST 날짜·00/06/12/18/24 눈금을 공통 축으로 사용한다. 현재 시각은 상단에 한 번만 표시하고 각 행에 검은 세로선을 반복하지 않는다. 다음 일정은 M/D HH:mm 개장·마감·재개로 통일하고 오늘/내일·남은 시간 문구는 쓰지 않는다. 거래 막대는 chart, 현재 진행 구간은 positive, 경매·예외는 warning 토큰을 사용하고 상태 문구를 병기한다. 기본/보조 글자는 label/caption, 줄 높이는 leading-label. 페이지 전체 폭 대신 시간표 컨테이너 폭으로 반응한다. 넓은 영역은 시장·시간축·다음 일정 3열, 좁은 영역은 시장·시간축 2열과 막대 아래 다음 일정으로 배치한다. 좁은 영역 눈금은 00/12/24로 줄이고 거래 구간 자체는 유지한다. 번호·장식 아이콘·설명 팝업은 추가하지 않는다. 펼친 본문은 날짜·시간축부터 시작하며 지역 선택·휴장 강조 띠·추가 시장 펼치기는 없다.

## 광고 영역

광고 단위는 콘텐츠와 구분되는 ‘광고’ 표기를 쓴다. 로컬 미리보기만 점선·위치 이름을 표시한다. 본문과의 간격은 space-6, 표기와 광고 사이는 space-2이며, 흰색 표면·muted 글자·line 테두리 토큰을 사용한다. 광고를 종목 행이나 추천 카드처럼 꾸미지 않는다. 광고 크기는 광고 규격을 위한 레이아웃 값이며 실제 Google 슬롯 안의 내용을 임의로 자르지 않는다. 배치와 화면별 크기는 PRODUCT_SPEC의 광고 배치가 기준이다.

## 화면 문구

### 공통 원칙

**서비스 방향(D014):** 사용자 화면은 탐색·판단·행동에 필요한 정보에 집중한다. 마지막 확인/조회 시각, 내부 갱신 주기·캐시·처리 과정 같은 운영 정보는 상시 노출하지 않는다. 실제 데이터의 의미·중요한 제한·실패/복구 행동은 필요한 위치에서 짧게 안내하고, 세부 비교 기준은 요청할 때 확인할 수 있게 한다. 개발자가 검증하기 편하다는 이유로 진단 정보를 제품 화면에 추가하지 않는다. 평가액 전용 지연 환율의 실제 적용 시각 한 줄은 위 승인된 예외이며, 정상 상태의 조회 시각·진단 정보까지 상시 표시하는 허용으로 확대하지 않는다.

### 실패 안내와 복구

- 오류는 **영향받는 기능·보존된 내용·실제로 가능한 다음 행동**만 짧게 보여준다. 서버 업데이트·DB 함수·HTTP 코드·예외 원문은 제품 문구로 표시하지 않고 개발 진단에 남긴다. 실패를 숨겨 성공처럼 보이게 하지 않는다.
- 거래 조회 제한/불러오기 실패는 보유자산·거래내역의 제목 아래, 거래 입력은 입력 영역에 한 번만 둔다. 시장·구루·관심종목·노트에 거래 저장소의 일반 경고를 반복하지 않는다. 조회 전용은 공통 surface/line/control 모서리·label 13px의 낮은 중립 안내로 표시하며, 서버 변경이 필요한 상태에 효과 없는 저장 재시도 버튼을 붙이지 않는다.
- **저장 결과 미확정·다른 기기와 충돌·서버 저장 후 기기 확인 실패**는 미해결 상태를 숨기지 않는다. 관련 화면에는 상태에 맞는 조작을 두고, 다른 화면에서는 짧은 상태와 `거래 확인` 링크만 유지한다. 정상 저장 진행은 경고색이 아닌 정중한 상태 알림이며, 입력 오류는 해당 폼에만 표시한다.
- `다시 불러오기`는 읽기만 한다. 미확정 변경을 포기하는 `저장된 기록 불러오기`는 영향 설명·취소가 있는 확인창을 먼저 띄운다. 충돌에는 같은 실패 요청을 반복하는 버튼을 두지 않는다. 통화 설정 실패에는 거래 복구 버튼 대신 통화 설정으로 연결한다.
- 모든 상태는 공통 글꼴·색·간격·44px 조작 영역을 재사용한다. 제목 반복·큰 노란 경고 카드·장식 배지·설명 펼침은 추가하지 않는다. 실제 오류는 `alert`, 조회 제한/저장 진행은 `status`로 읽히며, 확인창은 이름·초점 제한·Esc·닫은 뒤 조작 버튼 복귀를 제공한다.
- 원인 수정과 실패 표시 개선을 구분한다. 호환되지 않는 저장 구조를 오류 문구만 바꿔 출시 가능으로 판정하지 않는다. 배포 전 대상 DB 호환과 실제 저장 왕복을 확인해야 한다. 이 기준은 새 기능과 수정하는 실패 화면에 적용하며 기존 전 화면 준수 완료를 뜻하지 않는다.
- 근거(2026-09-24 확인): [Atlassian 메시지 기준](https://atlassian.design/foundations/content/designing-messages)은 전체 배너와 영역별 메시지를 구분하고, [Google Material 오류 기준](https://m1.material.io/patterns/errors.html)은 해결 가능한 행동을 안내하도록 한다. 공식 지침 확인이며 실제 모든 서비스 화면이나 Centbloom의 사용성 효과를 검증한 결과는 아니다.

### 정보와 행동의 중복 방지

- 시각의 표시 시간대는 `KST`로 쓴다. `한국시간`·`한국 시간` 문구는 사용하지 않는다. 같은 목록은 제목/도구줄에 한 번, 독립 상세는 시각 옆에 표시한다. 실제 날짜 계산은 Asia/Seoul 기준을 유지한다.

**제목·데이터·행동으로 이해되는 화면은 설명을 덧붙이지 않는다.** 문구를 지웠을 때 사용자의 행동·상태 판단·수치 해석이 달라지지 않으면 삭제한다.

- 개발자·에이전트가 의뢰인에게 전달할 작업 보고, 구현 이유, 내부 저장 구조, 미완성 사정, 개발 제안·예정 기능을 제품 페이지에 넣지 않는다. 해당 내용은 대화와 담당 문서에 기록한다. 실제 화면·시안·목업에 모두 적용한다.
- 문구의 독자는 이 대화의 의뢰인이 아니라 서비스를 사용하는 사람이다. 개발 대화를 모르는 사용자에게 행동·판단 가치가 없는 문구는 제거한다. 실제 오류·저장 범위·데이터 삭제 영향 등 사용에 필요한 조건은 해당 행동 옆에 짧게 남긴다.
- 미구현 기능을 작동하는 버튼·완료 상태처럼 보여주거나 빈 공간을 가짜 계정·거래·성과로 채우지 않는다. 샘플 체험·가상 포트폴리오 미리보기는 제공하지 않는다.
- 페이지는 기능을 바로 알 수 있는 제목과 필요한 행동으로 시작한다. 제목 밑 소개문, 같은 뜻의 영문 소제목, 슬로건은 기본으로 넣지 않는다.
- 카드·입력란·버튼을 다시 설명하지 않는다. 같은 정보는 필요한 위치에 한 번만 둔다. 설명만 있는 카드·열은 내용과 컨테이너를 함께 제거한다.
- ‘보고드립니다’, ‘이 종목 계속 보고받기’, ‘남다른 투자자를 위한’처럼 내부 경험 목표나 사용자를 띄우려는 의도를 문구로 드러내지 않는다. 준비된 정보의 순서·일관된 화면·지난 관심의 연속성으로 느낌을 만든다.
- 제목은 `관심종목`, `노트 작성`처럼 기능명으로, 버튼은 `추가`, `저장`, `삭제`처럼 행동으로 쓴다. 범위가 모호하면 `이 종목의 전체 거래 삭제`처럼 명시한다.
- 빈 상태는 짧은 상태와 필요한 다음 행동 하나로 구성한다. 주변에 같은 행동이 있으면 버튼·안내를 반복하지 않는다. 로딩·검색 결과 없음·오류는 구분한다.
- 입력 예시는 형식이 낯설 때 하나만 제공한다. placeholder로 label을 대체하지 않으며, 입력 뒤에도 필요한 조건은 입력란 옆에 남긴다.
- 계산법·용어 정의도 사용자 판단에 필요할 때만 도움말로 제공한다. 필요 없는 설명은 도움말로 옮기지 않고 생략한다. 기준 통화·기간 등 수치 해석을 실제로 바꾸는 조건과 중요한 누락은 수치 곁에 짧게 남기며 모든 계산 과정을 안내할 의무로 확대하지 않는다.
- 저장 위치와 백업 범위, 날짜 생략 시 기본값, 시세 지연/갱신 실패, 저장 오류, 삭제·전체 교체의 영향과 복구 가능 여부는 관련 행동·데이터 옆에 유지한다. 서버 이름·내부 처리 과정은 사용자의 판단에 필요한 결과로 바꿔 쓴다.
- 로그인·소개 화면도 기능 나열과 같은 약속의 반복을 없앤다. 사용자 작성 글·노트·댓글, 필수 고지, 검색용 metadata는 화면 장식 문구와 별도로 다룬다.
- 링크/카드가 클릭 가능함을 반복해서 나타내는 우상향 화살표(↗)는 붙이지 않는다. 실제 링크와 hover/focus·접근성 이름은 유지하고, 등락/매도처럼 데이터·동작 의미를 가진 방향 표시는 구분한다.
- 제목 계층, 입력 label, 아이콘 버튼의 접근성 이름, `aria-describedby` 참조, 오류·상태 알림을 지우거나 끊지 않는다.

| 피할 문구 | 현재 작성 기준 |
| --- | --- |
| `관심종목` 아래 “관심 있는 기업을 모으고…” | 설명 삭제 |
| “기다림에도, 나만의 기준을.” | `관심종목` |
| “이 종목 계속 보고받기”·“보고드립니다” | 기존 관심 별표·`종목 뉴스`처럼 중립적인 이름 |
| “어떤 종목을 거래했나요?” | `종목 검색` |
| “입력한 거래는 내 포트폴리오에 저장됩니다.” | 저장 버튼과 겹치는 안내 삭제 |
| “좋은 투자는 좋은 기록에서”와 긴 빈 화면 설명 | `작성한 노트 없음` + 필요한 작성 행동 |
| “날짜를 입력하지 않으면 오늘 기준으로 저장돼요…” | `날짜 미입력 시 오늘(KST)` |
| 계산 정의를 설명하는 상시 카드 | 불필요한 정의는 생략, 판단에 필요한 정의만 도움말. 필요한 단위·기준·실패 상태는 수치 곁에 유지 |

공통 `PageHeading`도 제목만으로 사용할 수 있어야 한다. 문구를 없앨 때는 JSX·불필요한 props·전용 스타일·빈 여백을 함께 정리하며 CSS로 숨기기만 하지 않는다. 남은 공간을 새 슬로건·아이콘·설명 카드로 채우지 않는다.

실제 후보와 적용 상태는 [화면 문구 정리](./PROJECT_STATUS.md#화면-문구-정리)에서 관리한다.

계정 설정의 통화는 계정 패널 안에 라벨과 작은 기본 select 한 줄로 둔다. 독립 카드·아이콘·소개문을 붙이지 않는다. 마우스 환경 컨트롤은 최소 32px, 터치 환경은 최소 44px을 확보한다. 데이터 관리는 기본 접힘이며 계정의 저장 구조/용량 진단표를 넣지 않는다.

## 글꼴과 글자 크기

본문은 아래 기본 글꼴, 코드·식별자만 고정폭 글꼴을 사용한다. 금액은 기본 글꼴에 `tabular-nums`를 사용한다. 화면마다 별도 서체를 선택하지 않는다.

브랜드명·한글·영문·숫자·메뉴·입력·차트는 **Wanted Sans Variable v1.0.3**로 통일한다. 본문 400, 라벨 500, 제목과 브랜드명 600을 기본으로 쓴다. 금액은 tabular-nums와 아래 크기 체계를 사용한다. 코드 전용 고정폭 글꼴과 기업 로고 고유 자산은 별도다.

[공식 v1.0.3](https://github.com/wanteddev/wanted-sans/tree/v1.0.3)의 원본 [WOFF2](./src/assets/fonts/WantedSansVariable.woff2) 1,289,292바이트와 [OFL 라이선스](./src/assets/fonts/OFL.txt)를 보관한다. 전체 한글·라틴 문자를 포함한 가변 파일 하나를 루트 layout의 next/font/local로 자체 제공하며, preload·파일 해시 URL·브라우저 캐시를 사용한다. display: swap으로 로딩 중 글자를 숨기지 않는다. 첫 방문에는 약 1.23MiB를 내려받고 이후 같은 파일을 재사용한다. 렌더링의 미세한 차이는 OS에 따라 남을 수 있다.

| 공통값 | 값 | 용도 |
| --- | --- | --- |
| `--cf-font-ui` | `var(--font-wanted-sans), Arial, "Malgun Gothic", sans-serif` | 기본 글꼴: 브랜드·한글·영문·숫자 Wanted Sans |
| `--cf-font-code` | `var(--font-geist-mono), monospace` | 코드·식별자만 |
| `--cf-text-caption` | `0.75rem` | 12px · 날짜·보조 수치 |
| `--cf-text-label` | `0.8125rem` | 13px · 표·짧은 라벨·버튼 |
| `--cf-text-body` | `0.9375rem` | 15px · 본문·댓글 |
| `--cf-text-input` | `1rem` | 16px · 입력·선택 컨트롤 |
| `--cf-text-section` | `1.125rem` | 18px · 카드/섹션 제목 |
| `--cf-text-title` | `1.75rem` | 28px · 페이지 제목; 모바일도 동일 |
| `--cf-text-metric` | `2rem` | 32px · 대표 자산 수치 |
| `--cf-text-display` | `2.5rem` | 40px · 소개 화면의 대표 문구만 |
| `--cf-weight-regular` | `400` | 본문 |
| `--cf-weight-medium` | `500` | 라벨·보조 버튼 |
| `--cf-weight-semibold` | `600` | 제목·주요 버튼 |
| `--cf-leading-body` | `1.6` | 본문 줄 높이 |
| `--cf-leading-label` | `1.4` | 짧은 라벨 줄 높이 |
| `--cf-leading-title` | `1.25` | 제목 줄 높이 |
| `--cf-tracking-body` | `0` | 본문 자간 |
| `--cf-tracking-title` | `-0.03em` | 큰 제목 자간 |

크기의 px 설명은 브라우저 기본 16px 기준이다. 모바일에서 본문·입력 크기를 줄여 맞추지 말고 줄바꿈과 배치를 조정한다. 긴 글은 본문 줄 높이, 제목은 제목 줄 높이를 쓴다. 12px 미만의 보조 글자와 기존 화면의 중간 크기들은 정리 대상이다.

## 색상

| 공통값 | 값 | 용도 |
| --- | --- | --- |
| `--cf-color-market-up` | `#d74353` | 모든 금융 상승·수익·플러스; + 기호 병기 |
| `--cf-color-market-down` | `#356ed5` | 모든 금융 하락·손실·마이너스; − 기호 병기 |
| `--cf-color-canvas` | `#f6f7f8` | 페이지 배경 |
| `--cf-color-surface` | `#ffffff` | 카드·입력 배경 / 진한 버튼의 글자 |
| `--cf-color-ink` | `#202329` | 제목·본문 |
| `--cf-color-backdrop` | `color-mix(in srgb, var(--cf-color-ink) 35%, transparent)` | 대화상자 뒤 배경 |
| `--cf-color-muted` | `#727680` | 보조 정보 |
| `--cf-color-line` | `#e6e8eb` | 구분선 |
| `--cf-color-soft` | `#f3f4f6` | 선택 전·호버 배경 |
| `--cf-color-action` | `#25282e` | 주요 버튼 |
| `--cf-color-action-hover` | `#40444c` | 주요 버튼 호버 |
| `--cf-color-focus` | `#6c747f` | 키보드 포커스 |
| `--cf-color-positive` | `#16856b` | 성공·정상 상태; 금융 수익에는 사용하지 않음 |
| `--cf-color-negative` | `#d65353` | 오류·삭제 경고; 금융 손실에는 사용하지 않음 |
| `--cf-color-positive-soft` | `#eaf5ef` | 성공 알림 배경 |
| `--cf-color-negative-soft` | `#fbeeee` | 오류 알림 배경 |
| `--cf-color-warning` | `#946a24` | 주의 문구 |
| `--cf-color-warning-soft` | `#fff9ed` | 주의 배경 |
| `--cf-color-selection` | `#dde6e2` | 텍스트 선택 |
| `--cf-color-allocation-1` | `#24457c` | 레거시 AllocationChart 전용 1 · 현재 런타임 미사용 |
| `--cf-color-allocation-2` | `#f0ad30` | 레거시 AllocationChart 전용 2 · 현재 런타임 미사용 |
| `--cf-color-allocation-3` | `#098b74` | 레거시 AllocationChart 전용 3 · 현재 런타임 미사용 |
| `--cf-color-allocation-4` | `#c75c93` | 레거시 AllocationChart 전용 4 · 현재 런타임 미사용 |
| `--cf-color-allocation-5` | `#59b8d9` | 레거시 AllocationChart 전용 5 · 현재 런타임 미사용 |
| `--cf-color-allocation-6` | `#8956c5` | 레거시 AllocationChart 전용 6 · 현재 런타임 미사용 |
| `--cf-color-allocation-7` | `#cb5f2e` | 레거시 AllocationChart 전용 7 · 현재 런타임 미사용 |
| `--cf-color-allocation-8` | `#9abf45` | 레거시 AllocationChart 전용 8 · 현재 런타임 미사용 |
| `--cf-color-allocation-9` | `#34373e` | 레거시 AllocationChart 전용 9 · 현재 런타임 미사용 |
| `--cf-color-allocation-10` | `#e5a7cd` | 레거시 AllocationChart 전용 10 · 현재 런타임 미사용 |
| `--cf-color-allocation-11` | `#728ee2` | 레거시 AllocationChart 전용 11 · 현재 런타임 미사용 |
| `--cf-color-allocation-12` | `#715233` | 레거시 AllocationChart 전용 12 · 현재 런타임 미사용 |
| `--cf-color-allocation-13` | `#a7d8ba` | 레거시 AllocationChart 전용 13 · 현재 런타임 미사용 |
| `--cf-color-allocation-14` | `#e16566` | 레거시 AllocationChart 전용 14 · 현재 런타임 미사용 |
| `--cf-color-allocation-15` | `#a6a9b0` | 레거시 AllocationChart 전용 15 · 현재 런타임 미사용 |
| `--cf-color-allocation-16` | `#5b2b57` | 레거시 AllocationChart 전용 16 · 현재 런타임 미사용 |
| `--cf-color-chart` | `#3b8879` | 주요 차트 계열; 장식에 사용하지 않음 |
| `--cf-color-comparison-1` | `#6979b8` | 수익률 비교 계열 1. 색·이름·선 강조를 함께 사용하며 등락색과 구분 |
| `--cf-color-comparison-2` | `#a87c45` | 수익률 비교 계열 2 |
| `--cf-color-comparison-3` | `#986eaa` | 수익률 비교 계열 3 |
| `--cf-color-comparison-4` | `#4b8da9` | 수익률 비교 계열 4 |
| `--cf-color-comparison-5` | `#bc7467` | 수익률 비교 계열 5 |
| `--cf-color-comparison-6` | `#7d8d44` | 수익률 비교 계열 6 |
| `--cf-color-comparison-7` | `#bb6890` | 수익률 비교 계열 7 |
| `--cf-color-comparison-8` | `#4d6593` | 수익률 비교 계열 8 |
| `--cf-color-comparison-9` | `#9b653b` | 수익률 비교 계열 9 |
| `--cf-color-comparison-10` | `#756199` | 수익률 비교 계열 10 |
| `--cf-color-comparison-11` | `#61817b` | 수익률 비교 계열 11 |
| `--cf-color-comparison-12` | `#8b6b70` | 수익률 비교 계열 12 |
| `--cf-color-brand` | `#bca26a` | 작은 브랜드 강조; 로고 원본 재색칠용 아님 |

모든 금융 등락·개인 손익은 market-up(빨강)/market-down(파랑)으로 통일한다(D017). Tailwind의 text-cf-market-up/down과 금융 positive/negative 클래스도 같은 토큰으로 연결한다. 0은 중립색이며 상승 화살표를 표시하지 않는다. 성공/오류 알림은 positive(초록)/negative(빨강) 토큰을 쓴다. 색만으로 부호를 전달하지 않는다. 종목 상세의 기간 등락률과 가격선은 같은 기간 방향색을 쓴다. 여러 종목·비교 대상·자산 추이의 계열 구분색은 등락 판정과 별개다.

## 간격과 공통 요소

| 공통값 | 값 | 용도 |
| --- | --- | --- |
| `--cf-space-1` | `4px` | 아이콘·문구 내부 간격 |
| `--cf-space-2` | `8px` | 관련 항목 간격 |
| `--cf-space-3` | `12px` | 조밀한 행 내부 간격 |
| `--cf-space-4` | `16px` | 모바일 여백·기본 행 내부 간격 |
| `--cf-space-6` | `24px` | 카드 내부·섹션 간격 |
| `--cf-space-8` | `32px` | 데스크톱 페이지 여백 |
| `--cf-space-12` | `48px` | 큰 섹션 구분 |
| `--cf-radius-control` | `8px` | 버튼·입력·작은 알림 |
| `--cf-radius-card` | `12px` | 카드·모달 |
| `--cf-radius-pill` | `9999px` | 아바타·짧은 상태 배지 |
| `--cf-shadow-dialog` | `0 12px 40px #20232916` | 떠 있는 팝오버·모달만 |

- 페이지 제목 28px/600, 카드 제목 18px/600, 본문 15px/400, 라벨·버튼 13px/500~600. 대표 수치만 32px.
- 카드: 흰 배경·구분선·12px 모서리·24px 내부 여백(모바일 16px). 기본 카드에는 그림자를 넣지 않는다.
- 통화처럼 선택지가 적은 설정은 라벨과 작은 선택 컨트롤을 한 행에 둔다. 큰 선택 카드·장식 아이콘·영문 번역·반복 설명으로 영역을 늘리지 않는다. 좁은 화면에서는 줄바꿈하되 터치 영역은 유지한다.
- 버튼: 8px 모서리. 주 행동은 진한 배경/흰 글자, 보조 행동은 흰 배경/구분선. 호버·포커스·비활성 상태를 함께 만든다.
- 입력: 16px, 8px 모서리, 오류 문구와 포커스 표시. 모바일의 단독 버튼·입력은 최소 높이 44px.
- 안내·빈 화면·오류는 같은 위치/간격을 재사용한다. 390px·1440px 및 글자 200% 확대에서 잘림·가로 넘침·겹침을 확인한다.

## 이미지와 아이콘

- 서비스 로고는 [BrandMark](./src/components/BrandMark.tsx), 종목 로고는 [AssetAvatar](./src/components/AssetAvatar.tsx)를 재사용한다.
- 서비스 마크는 투명 배경 자산을 사용해 배치한 표면의 배경이 그대로 보이도록 한다. 장미는 승인 원본의 비율·색·기울기를 유지한다. 찌그러뜨리기·색 필터·회전·잘라내기를 하지 않는다. 기업 로고도 `object-contain`으로 맞춘다.
- 순위 번호 아래 변동은 caption·muted로 표시해 주가 등락색과 구분한다. ▲/▼와 계단 수·신규·비교 불가를 툴팁과 접근성 설명으로 제공한다. 조회 시각·갱신 주기·반복 비교 푸터는 표시하지 않는다. 현재 비교 방식과 승인된 후속 목표는 [기본 순위 목록](./PRODUCT_SPEC.md#기본-순위-목록)을 따른다.
- 거래량 상위 제목에는 16px Lucide Flame 불꽃 아이콘과 market-up 색상을 사용한다.
- UI 아이콘은 기존 Lucide 선형 아이콘을 사용한다. 본문/버튼은 16px, 탐색은 20px, 빈 화면은 24px. 기본 선 굵기는 2, 색은 주변 글자에서 상속한다. 입력칸 안 로딩 아이콘은 오른쪽 수직 중앙에 고정하고 정렬용 이동과 회전을 같은 변환에 겹치지 않는다. 회전 중에도 중심 좌표와 입력칸 크기를 유지한다.
- 콘텐츠 썸네일은 16:9, 아바타는 1:1. 같은 목록에서 프레임을 섞지 않는다. 원본 이미지는 보존하고 썸네일 프레임만 자른다.
- 일반 이미지도 Next Image에 대체 문구와 크기(또는 fill)를 지정한다. 장식 이미지는 빈 alt를 사용한다.

## 작성과 검사

개발 중에는 바뀐 화면·스타일에 직접 연결된 좁고 빠른 확인만 하고, 전체 디자인 검사·화면 전수 검증은 푸시 직전에 모은다. 같은 변경을 여러 도구나 기기에서 반복 점검하지 않는다. 문서·공통값·관련 테스트 수정은 작업과 함께 한다. 실행 범위·재검사 조건은 [AGENTS 검사 원칙](./AGENTS.md#작업응답-원칙), 명령은 [README 검증](./README.md#검증)을 따른다.

수치의 실제 원본은 [design-tokens.css](./src/styles/design-tokens.css)다. 이 문서의 표와 값이 다르면 검사가 실패한다. 새로운 값은 공통값의 역할을 먼저 정하고 문서와 함께 수정한다.

```tsx
<h1 className="text-cf-title text-cf-ink">관심종목</h1>
<section className="rounded-cf-card border border-cf-line bg-cf-surface p-4 sm:p-6">
  {/* 내용 */}
</section>
```

CSS는 `color: var(--cf-color-ink)`, `font-size: var(--cf-text-body)`처럼 참조한다. 새 색 코드·픽셀 글자 크기·개별 서체를 직접 쓰지 않는다. Tailwind 간격은 0·1·2·3·4·6·8·12 단위와 auto, 굵기는 normal·medium·semibold, 글꼴은 sans·mono를 사용한다.

| 명령 | 판정 |
| --- | --- |
| `npm run check:design` | 기존 미정리 목록 대비 새로운 위반·증가, 문서와 값 불일치, 잘못된 공통값 참조를 실패 처리. 미정리 총수도 표시 |
| `npm run audit:design` | 기존 미정리까지 전부 검사. 하나라도 남으면 실패. 전체 목록은 `node tests/check-design.mjs --strict --json` |
| `node --test tests/design-system.test.mjs` | 잘못된 CSS·Tailwind·인라인 스타일·이미지와 예외 악용을 잡는지 재현 |
| `npm run lint` | 문서 검사 → 새 디자인 위반 검사 → ESLint |

[기존 미정리 목록](./tests/fixtures/design-baseline.json)은 첫 도입 시점의 코드 지문/개수다. **통과시키려고 목록을 늘리거나 재생성하지 않는다.** 해당 스타일을 수정할 때 공통값으로 이관하고 해당 지문을 제거한다. 해결된 지문이 남아 있어도 검사가 실패한다. 미정리 수는 직접 쓴 값과 규칙 위반 위치의 합계이며 화면의 시각적 오류 개수는 아니다. 새 파일에는 기존 면제가 적용되지 않으며 같은 위반을 복사해 늘려도 실패한다. 공식 Google 로그인 로고 색만 [사유 있는 예외](./tests/fixtures/design-exceptions.json)로 분리한다. 기존 미정리와 승인 예외를 섞지 않는다.

문구는 자동 스타일 검사로 판정하지 않는다. 제목·카드·폼·빈/오류 상태에서 중복, 행동에 필요한 조건, 접근성 이름/참조를 따로 확인한다. 실제 화면 검증을 할 때 390px·1440px에서 문구 제거 뒤 빈 카드·열·여백이 남지 않는지 확인한다.

자동 범위는 src의 CSS 선언, TS/JS의 정적 스타일 문자열·스타일 속성, 직접 사용하는 이미지 요소다. 계산으로 조합한 스타일·외부 라이브러리 내부·이미지 픽셀 색·실제 배치·역할 선택·가독성·색 대비·아이콘 크기 전체는 자동 보증하지 않는다. 필요한 화면 검증에서는 데스크톱/모바일의 제목·본문·버튼·이미지 역할과 위 공통 요소 조건을 직접 확인한다. ‘새 위반 없음’은 ‘전체 디자인 통일 완료’와 다른 결과다.
