<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## 작업·응답 원칙

- **모든 답변·제안·구현·검토는 사용자에게 동조하거나 이전 제안을 방어하는 대신, 근거에 따라 Centbloom의 지속 가능한 성공을 우선한다.** 승인된 제품 목표와 실제 사용자 경험·신뢰·재방문·수익·운영 비용을 함께 고려한다. 사용자 의견이나 에이전트 자신의 제안도 검토 대상이며, 불리한 근거를 생략하지 않는다. 성공을 명분으로 확정된 목표를 임의로 바꾸지 않는다.
- **답변 전에 핵심 전제와 반대 근거를 검토한다.** 사용자가 이 화면에서 해결할 일, 현재 구현의 장점, 변경 비용, 현행 유지 대안을 대조한다. 사용자 의견과 판단이 다르면 이유와 대안을 명확히 설명한다. 사용자 반응만으로 결론을 뒤집거나 새 디자인이 필요하다고 가정하지 않는다.
- **사실·연구 결과·추론·취향·미확인을 구분하고 근거의 적용 범위를 확인한다.** 연구 대상과 과제가 이 앱에 맞는지 확인하며, 경쟁사의 인지도·기능 존재나 일반 UX 지침만으로 특정 배치의 우월성·실제 이용 행동·사업 효과를 단정하지 않는다. 근거가 약하면 조건부 제안이나 현행 유지를 권고하고 판단을 바꿀 조건을 밝힌다.
- **이전 답변의 오류·과장은 명시적으로 정정한다.** 무엇을 잘못 전제했는지와 어떤 근거로 판단을 수정하는지 설명하고 관련 문서도 맞춘다. 매번 장황한 자기 점검을 출력하지 않고 결론·핵심 근거·중요한 한계를 전달한다. 지침에 기록했다는 사실을 모든 답변의 객관성이나 성공이 보장된다는 뜻으로 표현하지 않는다.

- 개발 완료 후 Codex 오른쪽 패널이나 인앱 브라우저에 미리보기 페이지를 자동으로 열지 않는다. 사용자에게 URL만 제공하며 화면을 열어달라는 명시적 요청이 있을 때만 연다. 브라우저 검증은 보이지 않는 창으로 실행한다.

- **검사는 푸시 직전에 모아서 실행한다.** 개발 중·매 작업 완료 때 lint·타입 검사·테스트·빌드·브라우저 전수 검증을 기본으로 실행하지 않는다. 오류 재현/수정에 필요한 최소 확인이나 사용자가 요청한 검사만 수행한다. 문서와 테스트의 작성·수정은 구현과 함께 하되 실행은 푸시 검사로 미룬다. 완료 보고에는 구현 완료와 검증 대기를 구분한다. `git push`의 pre-push 훅이 자동 검사를 실행하며 실패하면 푸시를 중단한다. 훅을 임의로 우회하지 않는다. 상세는 [README 검증](./README.md#검증)을 따른다.

- **개발 대화와 제품 화면을 구분한다.** 사용자에게 보고할 작업 내용·구현 사정·제안·예정 작업을 페이지나 목업의 문구로 넣지 않는다. 화면에는 실제 서비스 사용자의 행동·상태 판단에 필요한 내용만 둔다. 상세 기준은 [화면 문구](./DESIGN_SYSTEM.md#화면-문구)를 따른다.
- **대화와 문서는 요점만 쓴다.** 조사·판단 요청에는 에이전트가 확인 가능한 조사와 대안 평가를 끝내고 **판정·권고·사용자의 최종 선택**만 짧게 보고한다. 조사 방법이나 할 일 목록을 길게 나열하며 판단을 사용자에게 넘기지 않는다. 불가와 미확정을 구분하고 결론을 바꿀 외부 조건만 남긴다. 세부 근거는 담당 문서에 두며 요청받으면 설명한다. 반복 설명, 대화 경위, 장황한 배경은 생략한다.
- **사용자가 실제로 해결하려는 문제까지 처리한다.** 승인된 범위의 조사·개발·수정·초안·정리는 직접 수행하고, 확인할 수 있는 일을 “확인이 필요하다”거나 “원하면 하겠다”는 말로 남기지 않는다. 비용·오류·완료 여부는 실제 근거로 확인하고, 한 경로가 막히면 이용 가능한 다른 정당한 경로를 검토한다. 해결되지 않으면 시도·결과·남은 장애를 설명하고 본인 인증·개인 사실·최종 결정 등 대체 불가능한 최소 행동만 요청한다. 필요한 검사의 시점·범위는 위 검사 원칙을 따른다.
- 서비스 연결·개발 승인·예산 상한은 결제 승인이 아니다. 새 지출·유료 전환은 금액·필요성을 준비해 명시적 승인을 받아야 한다. 일반 가격표·사용 모델 설정·실제 가입 상품·청구액을 구분하고, 요금 발생 질문에는 연결된 계정의 가입/청구 근거부터 직접 확인한다.
- 실행 계획은 **기한·만들 기능/결과물·완료 기준·담당·필요한 결정**으로 제시한다. 개발 우선순위나 범위의 결정이 필요하면 준비된 작업 지시를 보여주고 해당 결정만 요청한다. 이미 승인된 작업은 다시 묻지 않는다.
- 다른 작업에 새 요구를 전달하기 전 진행 상태를 확인하고 **해당 전달에 대한 사용자 승인**을 받는다. 기능 논의·문서 반영을 전달/착수 승인으로 해석하지 않는다. 기존 승인 범위는 유지하되 새 요구는 계획에 보관한다.
- 공개 정보로 확인할 일반 수요·경쟁 서비스 조사는 직접 한다. 사용자의 조사·인터뷰나 외부 게시를 개발 착수 조건으로 만들지 않는다. 실제 제품의 이용·재방문 검증은 출시 후 실제 자료로 한다.
- 요약해도 확정 결정, 데이터·계산 의미, 실행 명령, 미해결 문제, 검증 범위는 지우거나 바꾸지 않는다.
- **기본 구현·수정 위치는 각 PC의 centbloom 원본 저장소의 로컬 `main`이다.** 집 PC는 `C:\Users\Siyun\Desktop\works\centbloom`, 회사 PC는 `C:\Users\proje\Desktop\개인용\centbloom`을 사용한다. PC를 바꾸면 GitHub 최신 main과 로컬 변경을 먼저 확인·통합한다. 명시적 작업 브랜치 지시가 없으면 별도 작업 브랜치·Git worktree를 만들지 않는다. 사용자 확인 주소는 이 루트에서 실행하는 `http://localhost:3000`이다.
- 사용자가 특정 작업에 로컬 브랜치를 명시하면 그 작업에 한해 같은 원본 저장소의 `codex/` 브랜치를 사용한다. 기본 main·localhost:3000 원칙은 다른 작업에 유지하되, 사용자가 해당 작업에 별도 주소/포트를 지정하면 그 주소를 사용한다. 기존 미커밋 변경을 보존한다.
- **매 작업 시작 시 실제 작업 위치와 브랜치를 확인하고 적합성을 판단한다.** `git rev-parse --show-toplevel`, `git branch --show-current`, `git status --short`로 확인하고 최신 사용자 지시·해당 작업의 지정 브랜치와 대조한다. 수정 직전과 최종 보고 직전에도 현재 브랜치를 재확인한다. 지정 브랜치와 다르면 기능 수정을 진행하지 않고 불일치를 먼저 알린다. 같은 폴더를 공유하는 작업이 있을 때 임의로 브랜치를 전환하거나 미커밋 변경을 다른 브랜치에 귀속된 것으로 취급하지 않는다.
- **모든 작업 진행·완료 보고의 첫 줄은 `작업 브랜치: 브랜치명`으로 쓴다.** 확인 전에는 `작업 브랜치: 확인 중`으로 표시하고 실제 확인값을 이어서 알린다. 지정 브랜치와 다르면 현재/지정 브랜치를 함께 표시한다. 작업 중 브랜치가 바뀌었다면 최종 보고에 실제 수정한 브랜치와 현재 브랜치를 구분하며, 미커밋 변경·커밋 완료·푸시 완료를 혼동하지 않는다.
- 이전 문서·스킬의 권장 방식·프로젝트 설정이 로컬 main 작업 원칙과 충돌하면 이 사용자 지시를 적용하고 해당 프로젝트 항목을 같은 작업에서 수정한다. 이미 승인된 이 원칙을 다시 확인받지 않는다. 다른 작업자의 변경을 덮어쓰거나 강제 초기화하지 않는다.
- 다른 작업자의 변경을 보존하고 요청과 무관한 코드·설정을 수정하지 않는다. 커밋·푸시·배포는 해당 사용자 지시를 따른다.
- **승인된 푸시·병합의 완료 절차에 해당 작업 브랜치 정리를 포함한다.** 최신 원격 main에 최종 작업이 반영됐는지 확인한 뒤, 별도 지시를 기다리지 않고 완료된 해당 원격/로컬 브랜치와 전용 worktree를 정리한다. GitHub의 PR 병합 후 원격 브랜치 자동 삭제 설정을 유지하며 이 설정이 로컬 폴더까지 삭제한다고 설명하지 않는다. 삭제 직전 미커밋·미추적·Git 제외 파일과 다른 작업/서버의 사용 여부를 확인하고 필요한 로컬 문서·설정·자료는 Git 제외 보관 위치에 먼저 보존한다. 다른 진행 중 작업·main·보관 지시 브랜치는 삭제하지 않는다. 일반 병합은 커밋 포함 여부, squash/rebase 병합은 병합된 PR의 최종 커밋과 실제 변경 반영을 확인한다. 미반영 변경·사용 여부가 불확실하면 정리를 보류하고 이유를 보고하며 강제 삭제로 우회하지 않는다. 사용자 직접 병합 이후의 로컬 정리는 다음 Codex 작업에서 확인해 수행하고 즉시 자동 실행된다고 약속하지 않는다.
- 사용하지 않는 개발 도구의 전용 지침·설정 파일을 추가하지 않는다.

## 문서 관리 기준

| 내용 | 기준 파일 |
| --- | --- |
| 사업 목표·수익모델·실행 기한·실적 | 로컬 전용 BUSINESS_MODEL.md |
| 현재 제품 결정·범위·핵심 이유 | [DECISIONS.md](./DECISIONS.md) |
| 거래소 일정 근거·지원 범위 | [MARKET_CALENDARS.md](./MARKET_CALENDARS.md) |
| 공개 홈 경쟁 서비스 조사 | [PUBLIC_HOME_RESEARCH.md](./PUBLIC_HOME_RESEARCH.md) |
| 기능·저장·계산 의미 | [PRODUCT_SPEC.md](./PRODUCT_SPEC.md) |
| 개발 일정·진행판·구현/검증/배포·남은 문제 | [PROJECT_STATUS.md](./PROJECT_STATUS.md) |
| 검색 유입 전략·조사 근거·우선순위 | [SEO_PLAN.md](./SEO_PLAN.md) |
| 배포·환경 설정 절차 | [CLOUDFLARE.md](./CLOUDFLARE.md) |
| 승인 브랜드·로고 자산 | [BRAND.md](./BRAND.md) |
| 화면 문구·글꼴·크기·색상·간격·이미지·검사 기준 | [DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md) |
| 소개·시작·문서 안내 | [README.md](./README.md) |
| 코드 지도·파일 책임·확장/검증 경로 | [DEVELOPMENT.md](./DEVELOPMENT.md) |
| 작업·문서 작성 규칙 | 이 AGENTS.md |

**모든 프로젝트 문서는 현재 기준을 유지하는 문서다. 변경할 때 기존 항목을 최신 결정으로 교체·통합하며, 날짜별 추가 기록·변경 이력·대화록·완료 보고서를 쌓지 않는다.** 이 원칙은 위 문서와 앞으로 추가하는 문서, 로컬 전용 사업 문서에 모두 적용한다. 폐기된 제안·대체된 기준·끝난 작업 과정은 삭제한다. 과거 정리본을 별도 문서로 만들어 옮기지 않는다. 현재 판단에 필요한 출처·확인일·계산 의미·유효한 완료 근거·미해결 문제는 보존한다.

1. 시작할 때 README의 문서 안내와 PROJECT_STATUS의 실행 일정/작업별 진행판에서 현재 상태·제약을 확인한다. 기능 추가·수정은 `npm run docs:guide -- "기능명 또는 코드 경로"` 또는 [작업별 시작점](./DEVELOPMENT.md#작업별-시작점)에서 **기능 명세 → 디자인 기준 → 구현 → 검증**을 찾는다. 검색 유입 작업은 SEO_PLAN의 관련 절을 읽는다. 무관한 기능·생성물은 필요할 때만 읽으며 수정 직전에 동시 변경을 확인한다.
2. 최신 사용자 지시를 우선한다. 결정·제안·검증 사실·미확인을 구분하고, 명세와 코드의 불일치는 PROJECT_STATUS에 적는다.
3. 작성 전에 기준표에서 담당 문서를 선택한다. **한 내용의 상세는 한 문서에서만 관리하고 다른 문서에는 링크한다.** 화면 문구 수정은 디자인 기준, 계산 변경은 기능 명세, 작업 상태는 진행판을 갱신한다. 모든 변경을 DECISIONS나 사업 문서에 복제하지 않는다. 새 기능·새 책임이 생길 때만 필요한 절·항목을 추가하며 기존 역할에 담을 수 없는 내용만 새 문서로 분리한다.
4. 문서를 고칠 때 같은 내용의 중복·대체된 설명도 함께 제거한다. 과거 코드·작업 경위는 필요한 경우 Git 이력이나 기존 재현 자료에서 찾는다. 현재 유효한 계약·호환/복구 조건·확정 실적·미해결 미달 판정은 해당 현재 항목에 짧게 보존한다. 과거 설명을 지우면서 미완료를 완료로 바꾸거나 목표를 완화하지 않는다.
5. 검증은 PROJECT_STATUS의 대상별 현재 항목을 갱신하고 날짜·환경·버전/범위·결과·남은 한계만 남긴다. 더 최신 근거로 대체된 기록과 반복 실행 로그는 삭제한다. 현재 변경을 검증하지 않은 옛 성공은 최신 성공으로 쓰지 않고 검증 대기로 표시한다. 조사 문서의 출처 확인일도 실제 재확인 없이 오늘 날짜로 바꾸지 않는다.
6. 문서를 신설·이름 변경·역할 변경하면 이 기준표, README 안내, 관련 참조를 함께 갱신한다. `npm run check:docs`는 푸시 검사에 모으며 관리 목록·상대 링크·제목 앵커·사업 문서 비공개·작업/변경 이력 전용 제목과 **기능별 명세/디자인/구현/검증 연결·새 기능 폴더/페이지/API의 등록 누락**을 확인한다. 제목 검사만으로 본문의 중복·낡은 설명을 판정할 수 없으므로 현재성·역할 중복·결정 모순은 직접 검토한다. 버전·설정은 package.json·package-lock.json·wrangler.jsonc가 기준이다.
7. BUSINESS_MODEL.md는 공개 저장소에 넣지 않는다. 수익모델·비용·제품 중심 방향·출시 범위·기한·측정에 영향이 있을 때 해당 사업 항목을 검토·갱신한다. 화면 문구·색상·개발 과정만 바뀌면 사업 문서를 수정하지 않는다. 제품 상세·개발 진행은 담당 문서를 링크하며 결정 문서 전체의 검토값이나 매번 ‘사업 영향 없음’ 기록을 남기지 않는다. 사업 검토가 필요한데 원본이 없으면 개인 계획을 추정·재생성하지 않고 미검토 상태와 다음 행동을 남긴다. 필요한 사업 검토는 자동 검사가 대신하지 않으며 다음 검토일·미해결 검토 누락도 현재 상태로 확인한다.
8. 전략·근거·공통 기준은 담당 문서에, 개별 구현의 상세 범위·재현 절차는 GitHub 이슈에 둔다. 개발 일정·현재 진행 상태·완료 증거의 요약은 PROJECT_STATUS가 단일 기준이다. 이슈는 해당 작업 ID와 기준 문서를 링크하고 별도 최신 일정표나 본문 전체를 복제하지 않는다. 작업 완료 시 바뀐 기준과 PROJECT_STATUS의 구현·검증 현황을 갱신한다. 이슈 등록을 개발 착수 승인으로 해석하지 않는다.

9. **개발 일정과 진행 상태는 PROJECT_STATUS.md 하나로 관리한다.** 매 작업 시작·종료에 해당 작업의 현재 행을 덮어 갱신하고 ID·상태·목표일·완료 근거·장애·다음 행동을 간결하게 둔다. 작은 후속 수정마다 새 작업 행이나 날짜별 보고서를 만들지 않는다. 구현·통합 검사·실제 계정/다중 기기·운영 반영·외부 조건을 구분한다. 마감 2영업일 전 위험이나 반복 장애는 짧게 알리고, 미해결 지연은 약속한 마감과 현재 예상일·이유를 같은 행에 둔다. 이슈와 사업 문서는 이 진행판을 참조하며 별도 일정표를 유지하지 않는다. 정기 점검은 읽기·상태 갱신 범위이며 코드·푸시·배포·외부 연락 승인을 확장하지 않는다.

## 코드 변경 안내

**사용자가 기능을 요청하면 문서 연결도 에이전트가 같은 작업에서 수행한다.** 사용자가 문서 이름을 지정하거나 매번 갱신을 요청할 필요가 없다. 구현 전에 기존 기능 명세와 디자인 유형을 선택하고, 완료 전에 기능 의미·코드 지도·현재 상태를 담당 문서에 반영한다. 새 기능은 [명세 형식](./DEVELOPMENT.md#기능-명세-작성-형식)과 작업별 시작점 한 행으로 등록한다. 문서화하려고 미확정 제품 방향을 임의로 정하거나 현재 코드의 오류를 승인된 기준으로 바꾸지 않는다. 확정 목표·현재 구현·미확인/미정은 구분한다.

파일 책임·공개 사용 창구·코드 추가 절차는 [DEVELOPMENT.md](./DEVELOPMENT.md)를 따른다. 페이지/Provider는 조립, 훅은 구독/수명, 계산은 순수 모듈, 저장은 저장소에 둔다. 기존 모듈을 수정하고 별도 저장 경로·폴링·중복 구현을 덧붙이지 않는다. 경로·책임·공개 창구를 바꾸면 코드 지도도 같은 변경에서 갱신한다.

화면·스타일 작업은 [DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md)를 먼저 읽고 공통값을 사용한다. 문서와 design-tokens.css를 함께 유지하며 `npm run check:design`은 푸시 검사에 포함한다(lint에도 포함). 기존 미정리 목록을 늘려 검사를 통과시키지 않는다. `npm run audit:design`의 전체 준수 결과와 새 위반 검사 결과를 구분하고, 실제 화면의 역할·배치·가독성은 필요한 화면 검증에서 별도로 확인한다. 매 수정 때 브라우저 전수 검증을 반복하지 않는다.
